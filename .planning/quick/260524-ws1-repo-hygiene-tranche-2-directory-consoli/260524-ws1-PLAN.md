---
phase: 260524-ws1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - user_data/versions/  # via git mv -> backend/user_data/versions/
  - cv-templates/         # via git mv -> backend/latex_templates/_source/
  - Dockerfile
  - docker-compose.yml
  - backend/services/storage_factory.py
  - backend/services/latex_compiler.py
  - backend/routes/templates.py
  - backend/scripts/migrate_to_dynamodb.py
  - .gitignore
  - README.md
  - docs/README.md
  - docs/DEPLOYMENT.md
  - docs/CONFIGURATION.md
  - docs/GETTING-STARTED.md
autonomous: true
requirements:
  - WS1-T2-USER-DATA-CONSOLIDATE
  - WS1-T2-CV-TEMPLATES-CONSOLIDATE
  - WS1-T2-DOCS-AND-IGNORES

must_haves:
  truths:
    - "Root user_data/ directory no longer exists after the move"
    - "Root cv-templates/ directory no longer exists after the move"
    - "backend/user_data/ contains both versions/ (9 JSONs, history preserved) and voice_profile.json"
    - "backend/latex_templates/_source/ contains the three template folders (deedy-resume, mcdowell-cv-master, med-length-proff-cv) with full git history"
    - "backend/latex_templates/ continues to contain the existing three .tex.j2 Jinja2 files alongside the new _source/ subdir"
    - "Backend pytest suite (non-slow) passes after both moves and code updates"
    - "Docker build context still resolves all paths (no dangling COPY from root cv-templates)"
    - ".gitignore ignores runtime data (backend/user_data/versions/ and backend/user_data/voice_profile.json) but allows the directory itself to be tracked"
    - "Single atomic commit captures all moves + ref updates with subject 'chore(repo-hygiene): tranche 2 — consolidate user_data and cv-templates into backend/'"
  artifacts:
    - path: "backend/user_data/versions/"
      provides: "Runtime CV JSON storage location (9 existing version files relocated with history)"
    - path: "backend/user_data/voice_profile.json"
      provides: "Runtime voice profile (already at this path; now the canonical location)"
    - path: "backend/latex_templates/_source/deedy-resume/"
      provides: "Deedy resume template source files (cls/sty/preview)"
    - path: "backend/latex_templates/_source/mcdowell-cv-master/"
      provides: "McDowell CV template source files"
    - path: "backend/latex_templates/_source/med-length-proff-cv/"
      provides: "Med-length professional CV template source files"
    - path: "backend/services/storage_factory.py"
      provides: "FileStorage base_dir computed via single '..' relative to backend/services/"
      contains: "os.path.join(os.path.dirname(__file__), \"..\", \"user_data\")"
    - path: "backend/services/latex_compiler.py"
      provides: "templates_dir pointing at backend/latex_templates/_source/"
      contains: "\"latex_templates\", \"_source\""
    - path: "backend/routes/templates.py"
      provides: "get_templates_dir() returning backend/latex_templates/_source/"
      contains: "\"latex_templates\", \"_source\""
    - path: "backend/scripts/migrate_to_dynamodb.py"
      provides: "USER_DATA_DIR using single '..' and a single voice_profile.json fallback path"
    - path: "Dockerfile"
      provides: "No COPY of root cv-templates/, single mkdir for /app/backend/user_data/versions"
    - path: "docker-compose.yml"
      provides: "Volume mount targets /app/backend/user_data"
      contains: "cv-maker-data:/app/backend/user_data"
    - path: ".gitignore"
      provides: "Runtime user_data file patterns under backend/, not root"
    - path: "SUMMARY.md"
      provides: "Quick-task summary including Docker volume migration steps for deployers"
  key_links:
    - from: "backend/services/storage_factory.py"
      to: "backend/user_data/"
      via: "os.path.join(__file__, '..', 'user_data') resolution"
      pattern: "\\.\\.\", \"user_data"
    - from: "backend/services/latex_compiler.py"
      to: "backend/latex_templates/_source/"
      via: "self.templates_dir initialization"
      pattern: "latex_templates.*_source"
    - from: "backend/routes/templates.py"
      to: "backend/latex_templates/_source/"
      via: "get_templates_dir()"
      pattern: "latex_templates.*_source"
    - from: "Dockerfile line 33 (COPY backend/)"
      to: "/app/backend/latex_templates/_source/"
      via: "Implicit copy via backend/ tree (no separate cv-templates COPY needed)"
      pattern: "COPY backend/ /app/backend/"
    - from: "docker-compose.yml volume"
      to: "FileStorage runtime path"
      via: "cv-maker-data:/app/backend/user_data binds to storage_factory base_dir"
      pattern: "cv-maker-data:/app/backend/user_data"
---

<objective>
Consolidate two top-level runtime/template directories under `backend/` so the repo root only contains source-of-truth code and config.

Move 1: `user_data/versions/` → `backend/user_data/versions/` (joins the already-stranded `voice_profile.json`).
Move 2: `cv-templates/` → `backend/latex_templates/_source/` (sits alongside the existing `.tex.j2` Jinja2 templates).

All code, Dockerfile, docker-compose, .gitignore, and current-state doc references update to match. History preserved via `git mv`. Single atomic commit at the end.

Purpose: Repo hygiene tranche 2 of 5 — eliminates "why are these at root?" cognitive cost and the stranded `voice_profile.json` inconsistency. Aligns with treat-`backend/` as a self-contained Python package.

Output: Two directory moves with full git history, all consumers updated, backend tests green, single commit, SUMMARY.md documenting the Docker volume breaking change for deployers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

<!-- Pre-recon facts (verified, do NOT re-discover): -->
<!-- Root user_data/ contains ONLY versions/ (9 CV JSON files). No voice_profile.json at root. -->
<!-- backend/user_data/voice_profile.json already exists (stranded). -->
<!-- Root cv-templates/ has 3 subdirs: deedy-resume/, mcdowell-cv-master/, med-length-proff-cv/. Tracked in git. -->
<!-- backend/latex_templates/ has 3 .tex.j2 files (different from source LaTeX, but related). -->

<key_files>
The executor needs to update these specific lines. Exact pre-change content (verified):

Dockerfile:34 — DELETE (redundant after move, since `backend/` is already copied at line 33):
```
COPY cv-templates/ /app/cv-templates/
```

Dockerfile:39-40 — SIMPLIFY:
```
&& mkdir -p /app/user_data/versions /app/backend/user_data \
&& chown -R appuser:appuser /app/user_data /app/backend/user_data \
```
Becomes:
```
&& mkdir -p /app/backend/user_data/versions \
&& chown -R appuser:appuser /app/backend/user_data \
```

docker-compose.yml:10 — UPDATE volume mount:
```
- cv-maker-data:/app/user_data
```
Becomes:
```
- cv-maker-data:/app/backend/user_data
```

backend/services/storage_factory.py:13 — DROP one `..`:
```
base_dir = os.path.join(os.path.dirname(__file__), "..", "..", "user_data")
```
Becomes:
```
base_dir = os.path.join(os.path.dirname(__file__), "..", "user_data")
```

backend/services/latex_compiler.py:45-47 — UPDATE templates path:
```
self.templates_dir = os.path.join(
    os.path.dirname(__file__), "..", "..", "cv-templates"
)
```
Becomes:
```
self.templates_dir = os.path.join(
    os.path.dirname(__file__), "..", "latex_templates", "_source"
)
```

backend/routes/templates.py:27-28 — UPDATE templates path:
```
def get_templates_dir() -> str:
    return os.path.join(os.path.dirname(__file__), "..", "..", "cv-templates")
```
Becomes:
```
def get_templates_dir() -> str:
    return os.path.join(os.path.dirname(__file__), "..", "latex_templates", "_source")
```

backend/scripts/migrate_to_dynamodb.py:21 — DROP one `..`:
```
USER_DATA_DIR = os.path.join(os.path.dirname(__file__), "..", "..", "user_data")
```
Becomes:
```
USER_DATA_DIR = os.path.join(os.path.dirname(__file__), "..", "user_data")
```

backend/scripts/migrate_to_dynamodb.py:55-70 — COLLAPSE redundant fallback (after the change above, both paths in `voice_paths` resolve to the same place; the dual lookup becomes meaningless):
```python
# --- Voice Profile (check both locations) ---
voice_paths = [
    os.path.join(USER_DATA_DIR, "voice_profile.json"),
    os.path.join(os.path.dirname(__file__), "..", "user_data", "voice_profile.json"),
]
for vp in voice_paths:
    if os.path.isfile(vp):
        try:
            ...
        except Exception as e:
            print(f"  SKIP voice profile {vp}: {e}", file=sys.stderr)
        break  # Only migrate the first one found
```
Becomes:
```python
# --- Voice Profile ---
voice_path = os.path.join(USER_DATA_DIR, "voice_profile.json")
if os.path.isfile(voice_path):
    try:
        with open(voice_path, "r") as f:
            voice = json.load(f)
        await storage.save_voice_profile(user_id, voice)
        migrated += 1
        print(f"  Migrated voice profile from {voice_path}")
    except Exception as e:
        print(f"  SKIP voice profile {voice_path}: {e}", file=sys.stderr)
```

.gitignore line 17 — LEAVE AS-IS (`graphify-out/`).

.gitignore line 261-262 — REPLACE the root pattern:
```
# User data (runtime-generated)
user_data/
```
Becomes:
```
# User data (runtime-generated; tracked dir, untracked contents)
backend/user_data/versions/
backend/user_data/voice_profile.json
```
</key_files>

<doc_update_targets>
Docs to update (current-state lines only — leave historical/CHANGELOG/ADR-narrative lines alone):

- README.md:108-112 — update tree diagram (remove `cv-templates/` block at root, change `user_data/` to `backend/user_data/`, mention `backend/latex_templates/` already has the `_source/` subdir).
- docs/README.md:80-91 — update the tree diagram block (remove `cv-templates/` line at root, change `user_data/` line).
- docs/DEPLOYMENT.md:54 — `| cv-maker-data | /app/user_data |` → `| cv-maker-data | /app/backend/user_data |`.
- docs/DEPLOYMENT.md:65 — `-v cv-maker-data:/app/user_data \` → `-v cv-maker-data:/app/backend/user_data \`.
- docs/DEPLOYMENT.md:165 — `user_data/versions/{uuid}.json` → `backend/user_data/versions/{uuid}.json`.
- docs/DEPLOYMENT.md:166 — `user_data/profile.json` → `backend/user_data/profile.json` (if present in that line range).
- docs/DEPLOYMENT.md:167 — `user_data/voice_profile.json` → `backend/user_data/voice_profile.json`.
- docs/DEPLOYMENT.md:168 — mount instruction `mount a persistent volume at /app/user_data` → `mount a persistent volume at /app/backend/user_data`.
- docs/DEPLOYMENT.md:212 — `from user_data/ into DynamoDB` → `from backend/user_data/ into DynamoDB`.
- docs/CONFIGURATION.md:202 — same update if line refers to volume mount or `user_data/` path describing current state.
- docs/GETTING-STARTED.md:71 — `# Storage — "file" stores CV versions as JSON files in user_data/` → `# Storage — "file" stores CV versions as JSON files in backend/user_data/`.

Lines NOT to touch:
- docs/CHANGELOG.md — historical entries describe past state. Leave alone unless a line asserts current behavior.
- docs/DECISIONS.md:262, 268, 526, 545-546, 585 — ADR narrative. Only update if the line is asserting "current state" (not historical context).
- Anywhere `user_data` appears as a route/function name (e.g., `get_user_data()`, `/user_data` URL) — code identifiers, do not touch.
</doc_update_targets>

<verification_facts>
- backend/services/file_storage.py uses `_base_dir/versions/` for `user_id=="local"` (line 16-18), `_base_dir/voice_profile.json` (line 27). After the move, with storage_factory pointing `_base_dir` at `backend/user_data/`, those paths resolve to `backend/user_data/versions/` and `backend/user_data/voice_profile.json` — both exist post-move. No file_storage.py change needed.
- Dockerfile line 33 already does `COPY backend/ /app/backend/`, which after the move will include the new `backend/latex_templates/_source/` tree. So line 34's separate `cv-templates/` copy becomes pure dead weight.
- The `cv-maker-data` Docker volume bind change is a deployment-breaking change for existing deployments — call it out in SUMMARY.md.
</verification_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move user_data/versions and update all user_data references</name>
  <files>user_data/versions/, backend/user_data/versions/, Dockerfile, docker-compose.yml, backend/services/storage_factory.py, backend/scripts/migrate_to_dynamodb.py</files>
  <action>
Perform Move 1 and update every code/Docker reference in lockstep. Do NOT touch docs or .gitignore yet (Task 3).

Steps:

1. Verify start state:
   ```
   ls user_data/         # should show: versions/
   ls user_data/versions/  # should list 9 .json files
   ls backend/user_data/   # should show: voice_profile.json
   ```

2. Run `git mv user_data/versions backend/user_data/versions` (preserves history of all 9 JSONs).

3. Remove the now-empty root `user_data/` parent directory:
   ```
   rmdir user_data
   ```
   (If `rmdir` fails because the directory is not empty, STOP and surface the contents — pre-recon said only `versions/` lives there; anything else is a surprise that needs the user.)

4. Edit `Dockerfile`:
   - Line 39: change `mkdir -p /app/user_data/versions /app/backend/user_data` → `mkdir -p /app/backend/user_data/versions`
   - Line 40: change `chown -R appuser:appuser /app/user_data /app/backend/user_data` → `chown -R appuser:appuser /app/backend/user_data`
   - (Defer the `cv-templates` line 34 deletion to Task 2 — keep changes per task scoped.)

5. Edit `docker-compose.yml` line 10:
   `- cv-maker-data:/app/user_data` → `- cv-maker-data:/app/backend/user_data`

6. Edit `backend/services/storage_factory.py` line 13:
   `base_dir = os.path.join(os.path.dirname(__file__), "..", "..", "user_data")`
   → `base_dir = os.path.join(os.path.dirname(__file__), "..", "user_data")`

7. Edit `backend/scripts/migrate_to_dynamodb.py`:
   - Line 21: `USER_DATA_DIR = os.path.join(os.path.dirname(__file__), "..", "..", "user_data")` → `USER_DATA_DIR = os.path.join(os.path.dirname(__file__), "..", "user_data")`
   - Lines 55-70 (the `voice_paths` block): collapse the dual-path loop into a single direct lookup. See `<key_files>` in context for the exact replacement block. The fallback path was meaningful only when `USER_DATA_DIR` resolved differently from the script-relative path; after step 7a they are identical, so the loop is redundant.

Do NOT modify file_storage.py — it computes paths relative to `_base_dir`, which storage_factory now passes as `backend/user_data/`. The existing `versions/` and `voice_profile.json` segments remain correct.

Per CLAUDE.md DnD-pattern note and `verbatimModuleSyntax`: not applicable here (Python only). `latex_escape`: not applicable (no template content changes).
  </action>
  <verify>
    <automated>cd backend && python -m pytest -x -m "not slow" 2>&1 | tail -30</automated>
    <!-- All non-slow backend tests must pass. If pytest exits non-zero, surface as blocker — do not proceed to Task 2. -->
    <!-- Also confirm: ls backend/user_data/versions/ shows 9 JSONs; root user_data/ no longer exists; git status shows the 9 files as renames (R) not deletes+adds. -->
  </verify>
  <done>
    - `user_data/` directory deleted from repo root
    - `backend/user_data/versions/` contains the 9 CV JSONs with preserved git history (verify via `git log --follow backend/user_data/versions/<one-of-the-files>.json` showing pre-move commits)
    - `backend/user_data/voice_profile.json` still exists (untouched)
    - Dockerfile lines 39-40 simplified (line 34 still has cv-templates COPY for now — that's Task 2)
    - docker-compose.yml mount targets `/app/backend/user_data`
    - storage_factory.py and migrate_to_dynamodb.py use the single-`..` path
    - Non-slow pytest suite green
  </done>
</task>

<task type="auto">
  <name>Task 2: Move cv-templates to backend/latex_templates/_source and update all template references</name>
  <files>cv-templates/, backend/latex_templates/_source/, Dockerfile, backend/services/latex_compiler.py, backend/routes/templates.py</files>
  <action>
Perform Move 2 and update every code/Docker reference. Doc/.gitignore changes still defer to Task 3.

Steps:

1. Verify start state:
   ```
   ls cv-templates/   # should show: deedy-resume mcdowell-cv-master med-length-proff-cv
   ls backend/latex_templates/  # should show: deedy-resume.tex.j2 mcdowell-cv.tex.j2 med-length-proff-cv.tex.j2
   ```

2. Run `git mv cv-templates backend/latex_templates/_source` (preserves history of all template source files: cls, sty, tex, preview images, font subdirs).

3. Verify the move resulted in:
   ```
   ls backend/latex_templates/         # should now show: _source/  deedy-resume.tex.j2  mcdowell-cv.tex.j2  med-length-proff-cv.tex.j2
   ls backend/latex_templates/_source/  # should show: deedy-resume mcdowell-cv-master med-length-proff-cv
   ls cv-templates 2>/dev/null && echo "FAIL: cv-templates still at root" || echo "OK: cv-templates removed"
   ```

4. Edit `Dockerfile`:
   - DELETE line 34 entirely: `COPY cv-templates/ /app/cv-templates/`. After the move, source files live under `backend/latex_templates/_source/` which is already copied by line 33's `COPY backend/ /app/backend/`. The separate copy creates a phantom `/app/cv-templates/` that no consumer reads.

5. Edit `backend/services/latex_compiler.py` lines 45-47:
   ```python
   self.templates_dir = os.path.join(
       os.path.dirname(__file__), "..", "..", "cv-templates"
   )
   ```
   →
   ```python
   self.templates_dir = os.path.join(
       os.path.dirname(__file__), "..", "latex_templates", "_source"
   )
   ```

6. Edit `backend/routes/templates.py` lines 27-28:
   ```python
   def get_templates_dir() -> str:
       return os.path.join(os.path.dirname(__file__), "..", "..", "cv-templates")
   ```
   →
   ```python
   def get_templates_dir() -> str:
       return os.path.join(os.path.dirname(__file__), "..", "latex_templates", "_source")
   ```

Note: `backend/services/latex_compiler.py` line 250-251 has a hardcoded `"med-length-proff-cv"` folder reference inside `self.templates_dir`; that uses the templates_dir attribute, so no separate change there. The folder name is preserved by the git mv.
  </action>
  <verify>
    <automated>cd backend && python -m pytest -x -m "not slow" 2>&1 | tail -30</automated>
    <!-- Non-slow pytest must pass. Tests in backend/tests/ that import latex_compiler or hit the templates route will exercise the new paths. If anything fails, surface as blocker — do not proceed to Task 3. -->
    <!-- Also confirm: backend/latex_templates/_source/{deedy-resume,mcdowell-cv-master,med-length-proff-cv}/ all exist; root cv-templates/ does not. -->
    <!-- Spot-check git history: git log --follow backend/latex_templates/_source/med-length-proff-cv/resume.cls should show pre-move commits. -->
  </verify>
  <done>
    - `cv-templates/` directory deleted from repo root
    - `backend/latex_templates/_source/` contains all 3 template folders with full git history preserved
    - Dockerfile line 34 deleted
    - latex_compiler.py and templates.py both reference the new path
    - Non-slow pytest suite green (route + compiler smoke coverage)
  </done>
</task>

<task type="auto">
  <name>Task 3: Update .gitignore + docs, run full backend test, single atomic commit</name>
  <files>.gitignore, README.md, docs/README.md, docs/DEPLOYMENT.md, docs/CONFIGURATION.md, docs/GETTING-STARTED.md</files>
  <action>
Surgical doc/.gitignore updates, then re-run tests, then create the single combined commit covering all three tasks' changes.

Steps:

1. Edit `.gitignore`:
   - Lines 261-262 currently:
     ```
     # User data (runtime-generated)
     user_data/
     ```
   - Replace with:
     ```
     # User data (runtime-generated; directory tracked, contents ignored)
     backend/user_data/versions/
     backend/user_data/voice_profile.json
     ```
   - Leave line 17 `graphify-out/` untouched (per pre-recon).
   - Note: the existing `backend/user_data/voice_profile.json` is currently TRACKED (it predates this change). Keep it tracked — the gitignore line above only prevents future contents from being added accidentally; it does not untrack anything already in the index. Confirm with `git status backend/user_data/voice_profile.json` showing no change after the gitignore edit.

2. Surgical doc updates — each line below describes the EXACT change. Be careful: only update lines that describe CURRENT on-disk paths. Skip historical/CHANGELOG/ADR narrative lines.

   `README.md`:
   - Lines 108-113 (project tree): remove the `cv-templates/` block at root (lines 108-111), change line 112 from `├── user_data/         # User profile and saved CV versions (JSON files)` to reflect that user data lives under `backend/`. Add a sub-bullet under `backend/` (line 102 area) for `latex_templates/_source/` if the existing `latex_templates/` line at 105 doesn't already imply it. Suggested replacement structure (be faithful to the original style):
     ```
     ├── backend/           # FastAPI backend server
     │   ├── routes/        # API endpoints
     │   ├── services/      # Business logic (Bedrock, LaTeX compiler)
     │   ├── latex_templates/  # Jinja2 .tex.j2 templates + _source/ LaTeX class files
     │   ├── user_data/     # User profile and saved CV versions (JSON files; runtime-generated)
     │   ├── tests/         # pytest test suite
     │   └── main.py        # FastAPI app
     └── docs/              # Project documentation
     ```

   `docs/README.md` lines 82-91: same shape — remove the root `cv-templates/` block (lines 85-88) and the root `user_data/` line (line 89). The simplified tree should mention `backend/user_data/` and `backend/latex_templates/_source/`.

   `docs/DEPLOYMENT.md`:
   - Line 54: `| cv-maker-data | /app/user_data | CV versions, user profiles (FileStorage mode) |` → `| cv-maker-data | /app/backend/user_data | CV versions, user profiles (FileStorage mode) |`
   - Line 65 (inside the `docker run` example): `-v cv-maker-data:/app/user_data \` → `-v cv-maker-data:/app/backend/user_data \`
   - Line 165: `- CV versions: \`user_data/versions/{uuid}.json\`` → `- CV versions: \`backend/user_data/versions/{uuid}.json\``
   - Line 166: `- User profile: \`user_data/profile.json\`` → `- User profile: \`backend/user_data/profile.json\``
   - Line 167: `- Voice profile: \`user_data/voice_profile.json\`` → `- Voice profile: \`backend/user_data/voice_profile.json\``
   - Line 168: `In Docker, mount a persistent volume at \`/app/user_data\` ...` → `In Docker, mount a persistent volume at \`/app/backend/user_data\` ...`
   - Line 202: `**\`cv-maker-data\` volume**: Persistent storage for \`user_data/\` ...` → `**\`cv-maker-data\` volume**: Persistent storage for \`backend/user_data/\` ...`
   - Line 212: `... copies CV versions, user profile, and voice profile from \`user_data/\` into DynamoDB.` → `... from \`backend/user_data/\` into DynamoDB.`

   `docs/CONFIGURATION.md` line 202 (and surrounding ~5 lines):
   - Read the file, find the line referring to `user_data/` as a current-state path or the `/app/user_data` mount, update to the `backend/user_data/` form. If the line is ambient prose ("data is stored in user_data/") update it. If it's quoting an env var or API path identifier, leave it.

   `docs/GETTING-STARTED.md` line 71:
   - `# Storage — "file" stores CV versions as JSON files in user_data/` → `# Storage — "file" stores CV versions as JSON files in backend/user_data/`

   `docs/CHANGELOG.md`: skip entirely. Historical narrative.
   `docs/DECISIONS.md`: skip the listed lines (262, 268, 526, 545-546, 585) UNLESS the specific line says something like "currently stored at user_data/" — re-read each. Most ADRs describe the rationale at the time of decision and stay valid.

3. Re-run the backend test suite (final check after all changes):
   ```
   cd backend && python -m pytest -x -m "not slow" 2>&1 | tail -30
   ```
   If non-zero exit, STOP. Do not commit. Surface the failure.

4. Run a final `git status` to review the staged-and-unstaged change set. Expected:
   - 9 renamed files under `backend/user_data/versions/` (R status)
   - Many renamed files under `backend/latex_templates/_source/` (R status, deedy-resume + mcdowell-cv-master + med-length-proff-cv tree)
   - Modifications to: Dockerfile, docker-compose.yml, .gitignore, README.md, backend/services/storage_factory.py, backend/services/latex_compiler.py, backend/routes/templates.py, backend/scripts/migrate_to_dynamodb.py, docs/README.md, docs/DEPLOYMENT.md, docs/CONFIGURATION.md, docs/GETTING-STARTED.md
   - No unexpected files (no stray `cv-templates/` or `user_data/` residue at root).

5. Create the SUMMARY.md at `.planning/quick/260524-ws1-repo-hygiene-tranche-2-directory-consoli/SUMMARY.md` BEFORE committing, so it's part of the same commit. Use the GSD summary template structure. Required content:
   - **What changed:** two `git mv` operations + ref updates across Docker/code/docs.
   - **Why:** repo hygiene tranche 2 of 5 — eliminate root clutter, fix stranded `voice_profile.json`.
   - **Breaking change for deployers (call out prominently):**
     ```
     ## ⚠ Docker Volume Migration Required

     The `cv-maker-data` volume mount target moved from `/app/user_data` to `/app/backend/user_data`.
     Existing deployments with persisted volumes must migrate their data:

     Option A (preferred — preserve volume):
       1. Stop the container: `docker compose down`
       2. Run a one-shot busybox to move the data inside the volume:
          docker run --rm -v cv-maker-data:/data busybox sh -c 'mkdir -p /data/backend && mv /data/* /data/backend/user_data 2>/dev/null; ls /data/backend/user_data'
          (Adjust the command to match your actual volume layout — inspect first with: docker run --rm -v cv-maker-data:/data busybox ls -la /data)
       3. Start with the new image: `docker compose up -d --build`

     Option B (clean slate):
       1. `docker compose down -v` (DESTROYS the volume)
       2. `docker compose up -d --build`
       3. Re-import any CVs / re-record voice profile.

     Local dev (no Docker volume): nothing to do — git mv handled the on-disk move.
     ```
   - **Files moved:** lists with old → new paths.
   - **Refs updated:** Dockerfile (lines 34, 39-40), docker-compose.yml, two backend services, route, migration script, .gitignore, 5 doc files.
   - **Verification:** non-slow backend pytest suite green.
   - **Out of scope:** DynamoDB storage path; architecture diagrams in `.planning/codebase/CONCERNS.md`; CHANGELOG/ADR historical entries.

6. Stage and commit ALL changes in a single atomic commit (per user explicit ask). Use the exact subject specified by the user, with a body that summarizes both moves and the breaking change:

   ```
   git add -A   # capture renames + modifications + new SUMMARY.md
   ```
   (Per CLAUDE.md "no `git add -A` if it might catch secrets" — verify with `git status` first that nothing in the staging set is `.env`-shaped or unrelated. The diff should be tightly scoped to the moves and ref updates listed above.)

   Then:
   ```
   git commit -m "$(cat <<'EOF'
chore(repo-hygiene): tranche 2 — consolidate user_data and cv-templates into backend/

Moves:
- user_data/versions/ -> backend/user_data/versions/ (9 CV JSON files,
  joins existing voice_profile.json)
- cv-templates/ -> backend/latex_templates/_source/ (sits alongside the
  Jinja2 .tex.j2 templates)

History preserved via git mv. Updates Dockerfile (drops redundant
cv-templates COPY, simplifies user_data mkdir/chown), docker-compose.yml
volume mount, two backend services (latex_compiler, storage_factory),
templates route, migrate_to_dynamodb script, .gitignore, README and four
docs/ pages.

Breaking change: existing Docker deployments with the cv-maker-data
volume must migrate data from /app/user_data to /app/backend/user_data.
See SUMMARY.md for the migration commands.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
   ```

7. Run `git status` after the commit to confirm a clean tree.
  </action>
  <verify>
    <automated>cd backend && python -m pytest -x -m "not slow" 2>&1 | tail -30 && git log -1 --stat | head -60 && git status</automated>
    <!-- 1. Backend tests must pass after all changes including doc updates (docs don't affect tests, but re-running confirms no accidental code edits leaked into Task 3). -->
    <!-- 2. git log -1 --stat shows the single commit with all expected files. -->
    <!-- 3. git status is clean (no leftover untracked/unstaged changes). -->
  </verify>
  <done>
    - .gitignore updated (root `user_data/` line replaced with backend-scoped patterns)
    - README.md tree diagram reflects new layout
    - docs/README.md tree updated; docs/DEPLOYMENT.md (8 lines), docs/CONFIGURATION.md (1 line), docs/GETTING-STARTED.md (1 line) all updated
    - SUMMARY.md exists in the quick task directory with prominent Docker volume migration callout
    - Single atomic commit on `feature/tuning-job` with the specified subject
    - `git status` clean
    - Final pytest run green
  </done>
</task>

</tasks>

<verification>
After all three tasks:

1. Tree-level sanity:
   ```
   ls user_data 2>/dev/null && echo FAIL || echo "OK: root user_data gone"
   ls cv-templates 2>/dev/null && echo FAIL || echo "OK: root cv-templates gone"
   ls backend/user_data/versions/ | wc -l   # expect 9
   test -f backend/user_data/voice_profile.json && echo "OK: voice profile present"
   ls backend/latex_templates/_source/   # expect 3 dirs
   ls backend/latex_templates/*.tex.j2 | wc -l   # expect 3
   ```

2. Code refs:
   ```
   grep -rn "\.\./\.\./cv-templates" backend/   # expect: 0 hits
   grep -rn "\.\./\.\./user_data" backend/      # expect: 0 hits
   grep -rn "\"cv-templates\"" backend/          # expect: 0 hits (string literal removed)
   grep -n "cv-templates" Dockerfile             # expect: 0 hits
   grep -n "/app/user_data" docker-compose.yml Dockerfile docs/DEPLOYMENT.md   # expect: 0 hits (all replaced with /app/backend/user_data)
   ```

3. Backend tests (authoritative gate):
   ```
   cd backend && python -m pytest -x -m "not slow"
   ```
   Must exit 0.

4. Git history preservation spot-check:
   ```
   git log --follow --oneline backend/user_data/versions/$(ls backend/user_data/versions/ | head -1) | head -5
   git log --follow --oneline backend/latex_templates/_source/med-length-proff-cv/resume.cls | head -5
   ```
   Both should show pre-move commits, not just the rename commit.

5. Single commit:
   ```
   git log -1 --pretty=format:"%s"
   # expect: chore(repo-hygiene): tranche 2 — consolidate user_data and cv-templates into backend/
   ```
</verification>

<success_criteria>
- Both directory moves complete with `git mv`, full history preserved
- All code/Docker/docs references in `<doc_update_targets>` and `<key_files>` updated
- No reference to root `user_data/` or root `cv-templates/` remains in any tracked file describing current state (CHANGELOG/ADR historical mentions allowed to stay)
- Non-slow backend pytest suite passes
- Single atomic commit with the specified subject covers all changes including SUMMARY.md
- SUMMARY.md prominently documents the Docker volume migration steps
- `git status` is clean post-commit
</success_criteria>

<output>
After completion, ensure these exist:
- `.planning/quick/260524-ws1-repo-hygiene-tranche-2-directory-consoli/SUMMARY.md` (written in Task 3, included in the commit)
- One new commit on `feature/tuning-job` with the user-specified subject
</output>
