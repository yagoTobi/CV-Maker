---
phase: quick-260415-q5j
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/direct-edit/DirectEditPage.tsx
autonomous: true
requirements:
  - fix-handletuneforjob-navigate-to-apply

must_haves:
  truths:
    - "Clicking Tune for Job in the editor navigates to /apply, not /dashboard"
    - "ApplyToJobScreen receives baseVersionId from navigation state and loads the correct version"
  artifacts:
    - path: "frontend/src/features/direct-edit/DirectEditPage.tsx"
      provides: "handleTuneForJob navigates to /apply with baseVersionId state"
  key_links:
    - from: "DirectEditPage.handleTuneForJob"
      to: "/apply"
      via: "navigate('/apply', { state: { baseVersionId } })"
      pattern: "navigate.*apply.*baseVersionId"
---

<objective>
Fix handleTuneForJob in DirectEditPage to navigate directly to /apply with the current
version's ID as baseVersionId in navigation state, bypassing the dashboard entirely.

Purpose: The user is already editing a specific CV — clicking "Tune for Job" should take
them straight to the apply-to-job flow for that CV, not to the dashboard where they would
have to pick a version they already have open.

Output: DirectEditPage.tsx with updated handleTuneForJob implementation.
</objective>

<execution_context>
@/Users/yagotobi/Documents/Code/GitHub/Projects/CV-Maker/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/yagotobi/Documents/Code/GitHub/Projects/CV-Maker/.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix handleTuneForJob to navigate to /apply with baseVersionId</name>
  <files>frontend/src/features/direct-edit/DirectEditPage.tsx</files>
  <action>
    In DirectEditPage, `handleTuneForJob` currently does `navigate('/dashboard')`.

    Change it to navigate to `/apply` with the active version's ID as `baseVersionId`
    in navigation state. The active version ID is available via `activeVersion?.id`
    from `useCVContext()` (already destructured at the top of the component).

    Replace the current implementation:
    ```ts
    const handleTuneForJob = useCallback(() => {
      navigate('/dashboard');
    }, [navigate]);
    ```

    With:
    ```ts
    const handleTuneForJob = useCallback(() => {
      navigate('/apply', { state: { baseVersionId: activeVersion?.id } });
    }, [navigate, activeVersion?.id]);
    ```

    No other changes needed. `activeVersion` is already in the destructured line at the
    top of the component. `ApplyToJobScreen` already reads `location.state.baseVersionId`
    at line 35 and uses it to load the base version — no changes needed there.
  </action>
  <verify>
    Run `cd /Users/yagotobi/Documents/Code/GitHub/Projects/CV-Maker/frontend && npx tsc --noEmit`
    to confirm no type errors. The `activeVersion` is typed as `CVVersion | null` so
    `activeVersion?.id` yields `string | undefined`, which matches the `baseVersionId?: string`
    interface in ApplyToJobScreen's LocationState.
  </verify>
  <done>
    `handleTuneForJob` navigates to `/apply` with `{ state: { baseVersionId: activeVersion?.id } }`.
    TypeScript reports zero errors. Clicking "Tune for Job" in the editor opens the apply-to-job
    flow pre-loaded with the currently edited version.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client navigation state | baseVersionId passed via React Router state — stays in-browser, no network exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q5j-01 | Tampering | navigation state baseVersionId | accept | React Router state is in-browser only; ApplyToJobScreen fetches the version via authenticated API call which validates ownership |
</threat_model>

<verification>
1. TypeScript compiles with zero errors: `cd frontend && npx tsc --noEmit`
2. In browser: open the direct editor, click "Tune for Job" in NavBar — confirm URL changes to `/apply` (not `/dashboard`)
3. ApplyToJobScreen loads and shows the base version that was being edited (step 1 pre-populated or version loaded correctly)
</verification>

<success_criteria>
- handleTuneForJob navigates to /apply with baseVersionId from activeVersion
- TypeScript compiles clean
- No changes needed in ApplyToJobScreen or NavBar
</success_criteria>

<output>
After completion, create `.planning/quick/260415-q5j-fix-handletuneforjob-navigate-to-apply-w/260415-q5j-SUMMARY.md`
</output>
