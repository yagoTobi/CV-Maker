---
phase: 06-route-integration
plan: 03
type: execute
wave: 2
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - frontend/src/features/form-builder/CVFormBuilder.tsx
  - frontend/src/features/form-builder/CVFormBuilder.module.css
  - frontend/src/features/form-builder/ImportBanner.tsx
  - frontend/src/features/form-builder/ImportBanner.module.css
  - frontend/src/features/form-builder/index.ts
  - frontend/src/features/form-builder/types.ts
  - frontend/src/features/form-builder/components/Field.tsx
  - frontend/src/features/form-builder/components/GripIcon.tsx
  - frontend/src/features/form-builder/components/useDrag.ts
  - frontend/src/features/form-builder/sections/index.ts
  - frontend/src/features/form-builder/sections/PersonalSection.tsx
  - frontend/src/features/form-builder/sections/WorkSection.tsx
  - frontend/src/features/form-builder/sections/EducationSection.tsx
  - frontend/src/features/form-builder/sections/SkillsSection.tsx
  - frontend/src/features/form-builder/sections/ProjectsSection.tsx
  - frontend/src/features/form-builder/sections/AwardsSection.tsx
  - frontend/src/features/form-builder/sections/AdditionalSectionView.tsx
  - frontend/src/features/editor/index.ts
  - frontend/src/features/editor/JobInput.tsx
  - frontend/src/features/editor/JobInput.module.css
  - frontend/src/features/editor/MatchAnalysis.tsx
  - frontend/src/features/editor/MatchAnalysis.module.css
  - frontend/src/features/editor/MatchSummaryBar.tsx
  - frontend/src/features/editor/MatchSummaryBar.module.css
  - frontend/src/features/editor/TailorPanel.tsx
  - frontend/src/features/editor/TailorPanel.module.css
  - frontend/src/features/cv-import/CVImportUpload.tsx
  - frontend/src/features/cv-import/CVImportUpload.module.css
  - frontend/src/features/cv-import/index.ts
  - frontend/src/hooks/useFormBuilder.ts
  - frontend/src/__tests__/resize-handle.test.tsx
  - frontend/src/__tests__/useFormBuilder.test.ts
autonomous: false
requirements:
  - ROUTE-01
  - ROUTE-02
  - ROUTE-05

must_haves:
  truths:
    - "frontend/src/features/form-builder/ directory does not exist"
    - "frontend/src/features/editor/ directory does not exist"
    - "frontend/src/features/cv-import/ directory does not exist"
    - "frontend/src/hooks/useFormBuilder.ts does not exist"
    - "frontend/src/__tests__/resize-handle.test.tsx does not exist"
    - "frontend/src/__tests__/useFormBuilder.test.ts does not exist"
    - "TypeScript compiles with zero errors after all deletions"
    - "All remaining tests pass after dead code removal"
  artifacts:
    - path: "frontend/src/features/form-builder/"
      provides: "DELETED -- form builder UI replaced by DirectEditPage"
    - path: "frontend/src/features/editor/"
      provides: "DELETED -- old LaTeX editor UI replaced by DirectEditPage"
    - path: "frontend/src/features/cv-import/"
      provides: "DELETED -- standalone import replaced by BuildChoiceScreen + EditorToolbar"
    - path: "frontend/src/hooks/useFormBuilder.ts"
      provides: "DELETED -- useDirectEditor has taken over all responsibilities"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "No references to deleted modules"
      via: "Verified by npx tsc --noEmit"
      pattern: "tsc.*noEmit.*exit 0"
---

<objective>
Remove all dead code: the form-builder feature folder, the editor feature folder, the cv-import feature folder, the useFormBuilder hook, and associated test files. Then verify the complete build flow end-to-end.

Purpose: Per D-18 (remove form-builder), D-19 (remove editor), D-20 (remove cv-import), D-22 (remove useFormBuilder after audit). This completes the transition from the old split-screen form editing UI to the new direct-edit web CV editor. Removing dead code prevents confusion, reduces bundle size, and marks the end of the old form-builder era.

Output: Clean codebase with only the web CV editor as the editing surface. Zero orphaned imports. TypeScript compiles cleanly. Human-verified end-to-end flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-route-integration/06-CONTEXT.md
@.planning/phases/06-route-integration/06-RESEARCH.md
@.planning/phases/06-route-integration/06-01-SUMMARY.md
@.planning/phases/06-route-integration/06-02-SUMMARY.md

@frontend/src/App.tsx

<interfaces>
<!-- Dead code audit from RESEARCH.md -- verified import dependencies -->

features/form-builder/ imported by:
- App.tsx (REMOVED in Plan 01 -- lazy import for CVFormBuilder deleted)
- __tests__/resize-handle.test.tsx (test file -- being deleted in this plan)

features/editor/ imported by:
- None (editor/index.ts barrel exists but zero consumers)

features/cv-import/ imported by:
- App.tsx (REMOVED in Plan 01 -- lazy import for CVImportUpload deleted)

hooks/useFormBuilder.ts imported by:
- features/form-builder/CVFormBuilder.tsx (being deleted in this plan)
- features/form-builder/types.ts (being deleted in this plan)
- features/form-builder/sections/PersonalSection.tsx (being deleted in this plan)
- __tests__/useFormBuilder.test.ts (test file -- being deleted in this plan)
- utils/entryFactories.ts (comment reference only, NOT an import)
- direct-edit/components/MedLengthTemplate.tsx (comment reference only, NOT an import)

Conclusion: After Plan 01 removes App.tsx references, ALL remaining importers of these modules are within the modules themselves or in test files being deleted. Safe to remove entirely.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead feature folders, hook, and test files</name>
  <files>
    frontend/src/features/form-builder/ (entire directory -- 17 files),
    frontend/src/features/editor/ (entire directory -- 9 files),
    frontend/src/features/cv-import/ (entire directory -- 3 files),
    frontend/src/hooks/useFormBuilder.ts,
    frontend/src/__tests__/resize-handle.test.tsx,
    frontend/src/__tests__/useFormBuilder.test.ts
  </files>
  <read_first>
    frontend/src/App.tsx,
    frontend/src/hooks/useFormBuilder.ts,
    frontend/src/hooks/index.ts
  </read_first>
  <action>
**Pre-deletion verification:** Before deleting anything, run `cd frontend && npx tsc --noEmit` to confirm the codebase compiles cleanly after Plan 01 and Plan 02 changes. This ensures we are starting from a clean state.

**Deletion order** (per RESEARCH.md Pattern 4 -- routes first, then feature folders, then hooks, then tests):

Plan 01 already removed route references from App.tsx. Now proceed with file deletions.

**Step 1: Delete `frontend/src/features/form-builder/` directory (per D-18).**

Delete the entire directory and all 17 files within:
```bash
rm -rf frontend/src/features/form-builder/
```

Files removed:
- CVFormBuilder.tsx, CVFormBuilder.module.css
- ImportBanner.tsx, ImportBanner.module.css
- index.ts, types.ts
- components/Field.tsx, components/GripIcon.tsx, components/useDrag.ts
- sections/index.ts, sections/PersonalSection.tsx, sections/WorkSection.tsx
- sections/EducationSection.tsx, sections/SkillsSection.tsx
- sections/ProjectsSection.tsx, sections/AwardsSection.tsx
- sections/AdditionalSectionView.tsx

Run `cd frontend && npx tsc --noEmit` to verify no orphaned imports.

**Step 2: Delete `frontend/src/features/editor/` directory (per D-19).**

Delete the entire directory and all 9 files within:
```bash
rm -rf frontend/src/features/editor/
```

Files removed:
- index.ts
- JobInput.tsx, JobInput.module.css
- MatchAnalysis.tsx, MatchAnalysis.module.css
- MatchSummaryBar.tsx, MatchSummaryBar.module.css
- TailorPanel.tsx, TailorPanel.module.css

Run `cd frontend && npx tsc --noEmit` to verify no orphaned imports.

**Step 3: Delete `frontend/src/features/cv-import/` directory (per D-20).**

Delete the entire directory and all 3 files within:
```bash
rm -rf frontend/src/features/cv-import/
```

Files removed:
- CVImportUpload.tsx, CVImportUpload.module.css, index.ts

Run `cd frontend && npx tsc --noEmit` to verify no orphaned imports.

**Step 4: Delete `frontend/src/hooks/useFormBuilder.ts` (per D-22).**

Per RESEARCH.md dead code audit: All remaining imports of useFormBuilder come from files deleted in Steps 1-3. No surviving production code imports it.

```bash
rm frontend/src/hooks/useFormBuilder.ts
```

**IMPORTANT:** Check `frontend/src/hooks/index.ts` barrel file. If it re-exports useFormBuilder, remove that re-export line. The barrel file may have a line like `export { useFormBuilder } from './useFormBuilder';` -- this line must be removed. Keep all other exports in the barrel file intact.

Run `cd frontend && npx tsc --noEmit` to verify.

**Step 5: Delete obsolete test files.**

```bash
rm frontend/src/__tests__/resize-handle.test.tsx
rm frontend/src/__tests__/useFormBuilder.test.ts
```

- `resize-handle.test.tsx` imports CVFormBuilder which no longer exists.
- `useFormBuilder.test.ts` tests the deleted useFormBuilder hook.

**Step 6: Final verification.** Run both:
```bash
cd frontend && npx tsc --noEmit
cd frontend && npx vitest run --reporter=verbose
```

Both must pass with zero errors. The remaining 13 test files in `__tests__/` should all pass since they test components/hooks that are still in the codebase (EditableField, EditableBulletList, useDirectEditor, useAutoSave, usePageBreak, etc.).

If any TypeScript errors appear after deletion, they indicate an orphaned import that was missed in the audit. Fix by removing the orphaned import line from the file that references the deleted module. The RESEARCH.md audit identified zero surviving imports, so this should not happen.
  </action>
  <verify>
    <automated>cd /Users/yagotobi/Documents/Code/GitHub/Projects/CV-Maker/frontend && npx tsc --noEmit 2>&1 | head -10 && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Directory frontend/src/features/form-builder/ does not exist (verified by `ls frontend/src/features/form-builder/ 2>&1` returning "No such file")
    - Directory frontend/src/features/editor/ does not exist
    - Directory frontend/src/features/cv-import/ does not exist
    - File frontend/src/hooks/useFormBuilder.ts does not exist
    - File frontend/src/__tests__/resize-handle.test.tsx does not exist
    - File frontend/src/__tests__/useFormBuilder.test.ts does not exist
    - frontend/src/hooks/index.ts does NOT contain "useFormBuilder"
    - npx tsc --noEmit exits 0 (zero TypeScript errors)
    - npx vitest run exits 0 (all remaining tests pass)
    - App.tsx does NOT contain "form-builder" or "cv-import" or "CVFormBuilder" or "CVImportUpload"
  </acceptance_criteria>
  <done>
    All dead code removed: form-builder feature folder (17 files), editor feature folder (9 files), cv-import feature folder (3 files), useFormBuilder hook, and 2 obsolete test files. TypeScript compiles cleanly. All remaining tests pass. The codebase contains only the web CV editor as the editing surface.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete build flow end-to-end</name>
  <files>N/A</files>
  <action>Present the human verification checklist. All automated work is complete in Task 1. This checkpoint verifies the full user-facing flow works correctly across all routes.</action>
  <what-built>
Phase 6 route integration is complete. The web CV editor (DirectEditPage) has replaced the form builder across all routes. A persistent NavBar appears on all working pages. Dead code has been removed.
  </what-built>
  <how-to-verify>
Start the frontend dev server: `cd frontend && npm run dev`
Start the backend: `cd backend && uvicorn main:app --host 0.0.0.0 --port 8000`

**Test 1: Build flow (ROUTE-05, D-04)**
1. Visit http://localhost:5173/
2. Verify: Landing page with NO nav bar (D-11)
3. Click "Build my CV"
4. Verify: BuildChoiceScreen at /build/start with NavBar visible (D-08). NavBar shows "CV Maker" + "My CVs" left, "+ New CV" right (D-10)
5. Click "Start from scratch"
6. Verify: TemplateSelector at /build. Three template cards visible (D-05)
7. Verify: deedy-resume and mcdowell-cv cards are visually disabled with "Coming soon" badge (D-06). Clicking them does nothing.
8. Click the med-length-proff-cv card
9. Verify: Web CV editor at /build/form (D-01). NavBar shows "CV Maker" + "My CVs" left, "Import CV" + "Download PDF" + save status right (D-09)
10. Type some text in the CV
11. Click "Download PDF" in the NavBar
12. Verify: PDF downloads successfully (ROUTE-05)

**Test 2: Dashboard navigation (ROUTE-03, D-14, D-15)**
1. Click "My CVs" in the NavBar -> Dashboard
2. If saved CVs exist: click "Edit" on a base CV
3. Verify: navigates to /build/form with the version loaded (D-15, ROUTE-03)
4. Go back to Dashboard
5. Click "Tune for a Job" on a base CV
6. Verify: navigates to /apply (3-step flow), NOT to /build/form (D-14)

**Test 3: NavBar navigation (D-16, D-17, D-13)**
1. From any working page, click "CV Maker" logo -> navigates to / (landing)
2. From any working page, click "My CVs" -> navigates to /dashboard
3. From dashboard, click "+ New CV" -> navigates to /build/start

**Test 4: Dead routes (ROUTE-02)**
1. Visit http://localhost:5173/direct-edit -> 404 page
2. Visit http://localhost:5173/import -> 404 page
  </how-to-verify>
  <verify>Human confirms all 4 test scenarios pass.</verify>
  <resume-signal>Type "approved" if all tests pass, or describe any issues found.</resume-signal>
  <done>Complete build flow works end-to-end. All routes render correct components. NavBar context-specific actions work. Dead routes return 404.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Dead code removal only. No new trust boundaries, no new inputs, no new data flows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Denial of Service | Dead code removal | accept | Deletion reduces attack surface by removing unused code paths. No risk introduced. |
</threat_model>

<verification>
1. `cd frontend && npx tsc --noEmit` -- zero TypeScript errors
2. `cd frontend && npx vitest run --reporter=verbose` -- all remaining tests pass
3. `ls frontend/src/features/form-builder/ 2>&1` -- "No such file or directory"
4. `ls frontend/src/features/editor/ 2>&1` -- "No such file or directory"
5. `ls frontend/src/features/cv-import/ 2>&1` -- "No such file or directory"
6. `ls frontend/src/hooks/useFormBuilder.ts 2>&1` -- "No such file or directory"
7. Human verification of complete build flow end-to-end
</verification>

<success_criteria>
- All dead code removed (form-builder, editor, cv-import, useFormBuilder, old tests)
- TypeScript compiles with zero errors
- All remaining tests pass
- Complete build flow works end-to-end (human verified)
- No references to deleted modules remain in surviving code
</success_criteria>

<output>
After completion, create `.planning/phases/06-route-integration/06-03-SUMMARY.md`
</output>
