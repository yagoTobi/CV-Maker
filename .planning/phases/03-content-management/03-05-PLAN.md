---
phase: 03-content-management
plan: 05
type: execute
wave: 2
depends_on: ["03-02", "03-04"]
files_modified:
  - frontend/src/features/direct-edit/components/EditableBulletList.tsx
  - frontend/src/features/direct-edit/components/EditableBulletList.module.css
  - frontend/src/features/direct-edit/components/ConfirmDialog.tsx
  - frontend/src/features/direct-edit/components/ConfirmDialog.module.css
  - frontend/src/features/direct-edit/components/SectionWrapper.module.css
  - frontend/src/features/direct-edit/components/EntryWrapper.module.css
autonomous: true
requirements: [CONT-02, CONT-01]
gap_closure: true

must_haves:
  truths:
    - "A subtle hint below the last bullet on focus indicates that pressing Enter adds a new bullet"
    - "The confirmation dialog has a backdrop overlay, stronger shadow, and entrance animation -- feels integrated with the CV"
    - "Hover-reveal controls (add, delete, toggle) have 18-20px icons, use --text-secondary color, and adequate padding for easy click targets"
  artifacts:
    - path: "frontend/src/features/direct-edit/components/EditableBulletList.tsx"
      provides: "Enter-to-add hint below last bullet (visible on focus)"
      contains: "Press Enter"
    - path: "frontend/src/features/direct-edit/components/EditableBulletList.module.css"
      provides: "Hint styling with muted text"
      contains: "hint"
    - path: "frontend/src/features/direct-edit/components/ConfirmDialog.module.css"
      provides: "Improved dialog with backdrop, stronger shadow, animation"
      contains: "backdrop"
    - path: "frontend/src/features/direct-edit/components/SectionWrapper.module.css"
      provides: "Larger icons and better contrast on toggle and add buttons"
      contains: "text-secondary"
    - path: "frontend/src/features/direct-edit/components/EntryWrapper.module.css"
      provides: "Larger icon and better contrast on delete button"
      contains: "text-secondary"
  key_links:
    - from: "frontend/src/features/direct-edit/components/EditableBulletList.tsx"
      to: "frontend/src/features/direct-edit/components/EditableBulletList.module.css"
      via: "hint element with conditional visibility on focus"
      pattern: "styles\\.hint"
---

<objective>
Fix three UX polish issues found during UAT: missing bullet-add hint, jarring confirmation dialog, and undersized/invisible hover controls.

Purpose: These issues affect discoverability and visual quality. New users cannot discover the Enter-to-add-bullet feature, the confirmation dialog clashes with the CV aesthetic, and hover controls are too small to notice. Fixing these completes the Phase 03 UX quality bar.

Output: EditableBulletList with Enter hint, polished ConfirmDialog with backdrop and animation, larger/more visible hover controls on SectionWrapper and EntryWrapper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-content-management/03-UAT.md
@.planning/phases/03-content-management/03-02-SUMMARY.md
@.planning/phases/03-content-management/03-04-SUMMARY.md

@frontend/src/features/direct-edit/components/EditableBulletList.tsx
@frontend/src/features/direct-edit/components/EditableBulletList.module.css
@frontend/src/features/direct-edit/components/ConfirmDialog.tsx
@frontend/src/features/direct-edit/components/ConfirmDialog.module.css
@frontend/src/features/direct-edit/components/SectionWrapper.tsx
@frontend/src/features/direct-edit/components/SectionWrapper.module.css
@frontend/src/features/direct-edit/components/EntryWrapper.tsx
@frontend/src/features/direct-edit/components/EntryWrapper.module.css
@frontend/src/styles/variables.css

<interfaces>
<!-- Design tokens from variables.css for consistent styling -->
```css
--text-secondary: #64748B;  /* Use instead of --text-muted (#94A3B8) for better contrast */
--text-muted: #94A3B8;      /* Current color -- too light for controls */
--accent: #3B82F6;
--error: #EF4444;
--bg-hover: #E2E8F0;
--shadow-md: 0 2px 8px rgba(0, 0, 0, 0.06);
--shadow-lg: 0 4px 16px rgba(0, 0, 0, 0.08);
--border-color: #E2E8F0;
--radius-sm: 6px;
```

<!-- Current control sizes from UAT diagnosis -->
SectionWrapper.module.css:
  .toggleButton: 16px SVG, 2px padding, var(--text-muted) color
  .addButton: 11pt font, opacity 0

EntryWrapper.module.css:
  .deleteButton: 14px SVG, 2px padding, var(--text-muted) color

Target improvements per UAT gap diagnosis:
  - Icons: 18-20px (up from 14-16px)
  - Color: var(--text-secondary) instead of var(--text-muted)
  - Padding: 4-6px (up from 2px)
  - Add subtle background on hover for better affordance
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Enter-to-add hint on EditableBulletList and increase control sizes</name>
  <files>
    frontend/src/features/direct-edit/components/EditableBulletList.tsx,
    frontend/src/features/direct-edit/components/EditableBulletList.module.css,
    frontend/src/features/direct-edit/components/SectionWrapper.tsx,
    frontend/src/features/direct-edit/components/SectionWrapper.module.css,
    frontend/src/features/direct-edit/components/EntryWrapper.module.css
  </files>
  <read_first>
    - frontend/src/features/direct-edit/components/EditableBulletList.tsx (current structure, where to add hint)
    - frontend/src/features/direct-edit/components/EditableBulletList.module.css (current styles)
    - frontend/src/features/direct-edit/components/SectionWrapper.tsx (SVG icon sizes to update)
    - frontend/src/features/direct-edit/components/SectionWrapper.module.css (toggle + add button styles)
    - frontend/src/features/direct-edit/components/EntryWrapper.tsx (SVG icon size to update)
    - frontend/src/features/direct-edit/components/EntryWrapper.module.css (delete button styles)
  </read_first>
  <action>
    **Fix 1: Enter-to-add bullet hint (MINOR - UAT Test 3)**

    Update `EditableBulletList.tsx`:
    1. Add `useState` to React imports (already has useRef, useEffect, useCallback).
    2. Add focus tracking state: `const [hasFocus, setHasFocus] = useState(false);`
    3. Add `onFocus` and `onBlur` handlers on the bulletList container div:
       ```tsx
       <div
         className={styles.bulletList}
         onFocus={() => setHasFocus(true)}
         onBlur={(e) => {
           // Only blur if focus leaves the bullet list entirely
           if (!e.currentTarget.contains(e.relatedTarget as Node)) {
             setHasFocus(false);
           }
         }}
       >
       ```
    4. After the bullet items map, add a hint element:
       ```tsx
       {hasFocus && (
         <div className={styles.hint}>
           Press Enter to add a new bullet
         </div>
       )}
       ```

    Update `EditableBulletList.module.css`:
    Add hint styles:
    ```css
    .hint {
      color: var(--text-muted, #94A3B8);
      font-family: 'IBM Plex Sans', -apple-system, sans-serif;
      font-size: 10px;
      padding: 2px 0 0 1.5em;
      user-select: none;
      opacity: 0.7;
    }
    ```
    Note: Use IBM Plex Sans (chrome font) for the hint since it is UI guidance, not CV content. The `padding-left: 1.5em` aligns with the bullet text (bulletList has `padding-left: 1em` + bulletMarker `margin-right: 0.5em`).

    **Fix 2: Increase control sizes and contrast (MINOR - UAT Test 8)**

    Update `SectionWrapper.tsx`:
    - Change EyeIcon SVG from `width="16" height="16"` to `width="18" height="18"`
    - Change EyeOffIcon SVG from `width="16" height="16"` to `width="18" height="18"`

    Update `SectionWrapper.module.css`:
    - `.toggleButton`: Change `color: var(--text-muted)` to `color: var(--text-secondary)`. Change `padding: 2px` to `padding: 4px`. Add `border-radius: 4px`.
    - `.toggleButton:hover`: Add `background: var(--bg-hover)` for better affordance in addition to color change.
    - `.addButton`: Change `color: var(--text-muted)` to `color: var(--text-secondary)`. Change `font-size: 11pt` to `font-size: 10pt` (slightly smaller but more visible with better color contrast).
    - `.addButton:hover`: Add `background: var(--bg-hover)` and `border-radius: 4px`.

    Update `EntryWrapper.tsx` (if needed -- check SVG size):
    - Change X icon SVG from `width="14" height="14"` to `width="16" height="16"`. (Not as large as section controls since delete is per-entry and should be subtler.)

    Update `EntryWrapper.module.css`:
    - `.deleteButton`: Change `color: var(--text-muted)` to `color: var(--text-secondary)`. Change `padding: 2px` to `padding: 4px`. Add `border-radius: 4px`.
    - `.deleteButton:hover`: Keep `color: var(--error)`. Add `background: var(--error-light, #FEE2E2)` for red-tinted hover background.
  </action>
  <verify>
    <automated>cd /Users/yagotobi/Documents/Code/GitHub/Projects/CV-Maker/frontend && npx vitest run --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <done>
    Bullet lists show "Press Enter to add a new bullet" hint when focused. Toggle, add, and delete controls use --text-secondary color (darker), larger icons (18px/16px), more padding (4px), and subtle hover backgrounds. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Polish ConfirmDialog with backdrop, stronger shadow, and entrance animation</name>
  <files>
    frontend/src/features/direct-edit/components/ConfirmDialog.tsx,
    frontend/src/features/direct-edit/components/ConfirmDialog.module.css
  </files>
  <read_first>
    - frontend/src/features/direct-edit/components/ConfirmDialog.tsx (current structure)
    - frontend/src/features/direct-edit/components/ConfirmDialog.module.css (current styles)
    - frontend/src/features/direct-edit/components/EntryWrapper.tsx (how ConfirmDialog is rendered -- positioned inside entryWrap)
    - frontend/src/styles/variables.css (shadow-md, shadow-lg, radius-sm for consistent tokens)
  </read_first>
  <action>
    **Fix: ConfirmDialog visual polish (COSMETIC - UAT Test 4)**

    Per UAT diagnosis: dialog overlaps content, minimal shadow, no backdrop/overlay, IBM Plex Sans clashes with EB Garamond, no entrance animation.

    Update `ConfirmDialog.tsx`:
    1. Add a backdrop overlay element that sits behind the dialog:
       ```tsx
       export function ConfirmDialog({ message, onConfirm, onCancel }: ConfirmDialogProps) {
         return (
           <>
             <div className={styles.backdrop} onClick={onCancel} />
             <div className={styles.dialog} role="alertdialog" aria-label={message}>
               <p className={styles.message}>{message}</p>
               <div className={styles.actions}>
                 <button className={styles.cancelBtn} onClick={onCancel}>
                   Cancel
                 </button>
                 <button className={styles.deleteBtn} onClick={onConfirm}>
                   Delete
                 </button>
               </div>
             </div>
           </>
         );
       }
       ```
    2. Note: Swap button order to put Cancel first (more standard -- dangerous action on the right).
    3. The backdrop covers the immediate area and catches clicks to dismiss.

    Update `ConfirmDialog.module.css`:
    ```css
    /* Backdrop -- semi-transparent overlay behind dialog */
    .backdrop {
      position: fixed;
      inset: 0;
      background: rgba(0, 0, 0, 0.08);
      z-index: 9;
    }

    .dialog {
      position: absolute;
      top: -4px;
      right: -24px;
      z-index: 10;
      background: #fff;
      border: 1px solid var(--border-color);
      border-radius: var(--radius-sm);
      box-shadow: var(--shadow-lg);
      padding: 10px 14px;
      font-family: 'IBM Plex Sans', -apple-system, sans-serif;
      font-size: 13px;
      white-space: nowrap;
      animation: dialogEnter 0.15s ease-out;
    }

    @keyframes dialogEnter {
      from {
        opacity: 0;
        transform: translateY(-4px) scale(0.97);
      }
      to {
        opacity: 1;
        transform: translateY(0) scale(1);
      }
    }

    .message {
      margin: 0 0 8px 0;
      color: var(--text-primary);
      font-weight: 500;
    }

    .actions {
      display: flex;
      gap: 8px;
      justify-content: flex-end;
    }

    .deleteBtn {
      background: var(--error);
      color: #fff;
      border: none;
      border-radius: 4px;
      padding: 5px 14px;
      cursor: pointer;
      font-family: 'IBM Plex Sans', -apple-system, sans-serif;
      font-size: 13px;
      font-weight: 500;
    }

    .deleteBtn:hover {
      background: #dc2626;
    }

    .cancelBtn {
      background: none;
      border: 1px solid var(--border-color);
      border-radius: 4px;
      padding: 5px 14px;
      cursor: pointer;
      font-family: 'IBM Plex Sans', -apple-system, sans-serif;
      font-size: 13px;
      color: var(--text-secondary);
    }

    .cancelBtn:hover {
      background: var(--bg-tertiary);
      color: var(--text-primary);
    }
    ```

    Key changes from current:
    - Added `.backdrop` with subtle semi-transparent overlay (0.08 opacity -- barely visible but catches clicks)
    - Changed `box-shadow` from `var(--shadow-sm)` to `var(--shadow-lg)` for stronger elevation
    - Changed `top: 0` to `top: -4px` to offset slightly above the delete button
    - Added `animation: dialogEnter 0.15s ease-out` for smooth entrance
    - Added `font-weight: 500` to message for emphasis
    - Increased padding from 8px/12px to 10px/14px for breathing room
    - Increased button padding from 4px/12px to 5px/14px
    - Added `justify-content: flex-end` to actions for right-aligned buttons
    - Cancel button now uses `--text-secondary` color (not `--text-primary`) for visual hierarchy
  </action>
  <verify>
    <automated>cd /Users/yagotobi/Documents/Code/GitHub/Projects/CV-Maker/frontend && npx vitest run src/__tests__/EntryWrapper.test.tsx --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <done>
    ConfirmDialog has a light backdrop overlay, stronger shadow elevation, smooth entrance animation, better button sizing and spacing. Feels integrated rather than jarring. All tests pass.
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/yagotobi/Documents/Code/GitHub/Projects/CV-Maker/frontend && npx vitest run --reporter=verbose && npx tsc --noEmit
```
All tests pass, TypeScript compiles cleanly. Manual verification: bullet hint appears on focus, dialog has backdrop and animation, controls are clearly visible on hover.
</verification>

<success_criteria>
- Focused bullet list shows "Press Enter to add a new bullet" hint below last bullet
- Hint disappears when focus leaves the bullet list
- ConfirmDialog has a subtle backdrop overlay that dismisses on click
- ConfirmDialog appears with a smooth entrance animation
- ConfirmDialog has stronger shadow (shadow-lg) and more padding
- Toggle buttons use 18px icons with --text-secondary color and hover background
- Add buttons use --text-secondary color and hover background
- Delete buttons use 16px icons with --text-secondary color and red-tinted hover background
- All existing tests pass, no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-management/03-05-SUMMARY.md`
</output>
