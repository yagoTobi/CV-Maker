---
phase: 01-data-model-prep
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - frontend/src/hooks/useFormBuilder.ts
  - frontend/src/utils/formDataPatch.ts
  - frontend/src/features/form-builder/sections/WorkSection.tsx
  - frontend/src/features/form-builder/sections/EducationSection.tsx
  - frontend/src/features/form-builder/sections/SkillsSection.tsx
  - frontend/src/features/form-builder/sections/ProjectsSection.tsx
  - frontend/src/features/form-builder/sections/AdditionalSectionView.tsx
  - frontend/src/__tests__/useFormBuilder.test.ts
  - frontend/src/__tests__/formDataPatch.test.ts
autonomous: true
requirements: [DATA-01, DATA-02]

must_haves:
  truths:
    - "Empty factory functions generate entries with unique IDs"
    - "Bullet CRUD operations produce BulletItem objects (not bare strings)"
    - "Skills text update produces SkillItem objects preserving IDs for unchanged skills"
    - "UI sections render bullet.text and skill.text (not the object itself)"
    - "formDataPatch handles BulletItem at leaf nodes when applying tailor changes"
    - "TypeScript compiles with zero errors"
    - "All frontend tests pass"
  artifacts:
    - path: "frontend/src/hooks/useFormBuilder.ts"
      provides: "Factory functions with generateId(), CRUD ops producing BulletItem/SkillItem"
      contains: "generateId"
    - path: "frontend/src/utils/formDataPatch.ts"
      provides: "BulletItem-aware path resolution for tailor changes"
      contains: "BulletItem"
    - path: "frontend/src/__tests__/formDataPatch.test.ts"
      provides: "Tests for BulletItem leaf handling in applyTailorChanges"
      contains: "BulletItem"
    - path: "frontend/src/__tests__/useFormBuilder.test.ts"
      provides: "Updated test expectations with id fields and BulletItem structures"
      contains: "generateId"
  key_links:
    - from: "frontend/src/hooks/useFormBuilder.ts"
      to: "frontend/src/utils/idHelpers.ts"
      via: "import { generateId } from '../utils/idHelpers'"
      pattern: "import.*generateId.*idHelpers"
    - from: "frontend/src/utils/formDataPatch.ts"
      to: "frontend/src/types/index.ts"
      via: "import type { BulletItem }"
      pattern: "import.*BulletItem"
    - from: "frontend/src/features/form-builder/sections/WorkSection.tsx"
      to: "frontend/src/types/index.ts"
      via: "bullet.text access pattern"
      pattern: "bullet\\.text"
---

<objective>
Update all frontend consumers to work with ID-bearing CVFormData: factory functions, CRUD operations, form data patch utility, UI sections, and frontend tests.

Purpose: The form builder, tailor change application, and all UI sections must operate on BulletItem/SkillItem structures instead of bare strings. This completes the frontend side of the data model migration.
Output: useFormBuilder produces ID-bearing entries, UI sections render .text, formDataPatch handles structured leaves, all frontend tests pass with zero TypeScript errors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-model-prep/01-CONTEXT.md
@.planning/phases/01-data-model-prep/01-RESEARCH.md
@.planning/phases/01-data-model-prep/01-01-SUMMARY.md

@frontend/src/hooks/useFormBuilder.ts
@frontend/src/utils/formDataPatch.ts
@frontend/src/features/form-builder/sections/WorkSection.tsx
@frontend/src/features/form-builder/sections/EducationSection.tsx
@frontend/src/features/form-builder/sections/SkillsSection.tsx
@frontend/src/features/form-builder/sections/ProjectsSection.tsx
@frontend/src/features/form-builder/sections/AdditionalSectionView.tsx
@frontend/src/__tests__/useFormBuilder.test.ts

<interfaces>
<!-- From Plan 01 outputs — executor needs these contracts -->

From frontend/src/types/index.ts (updated):
```typescript
export interface BulletItem {
  id: string;
  text: string;
}

export interface SkillItem {
  id: string;
  text: string;
}

export interface WorkEntry {
  id: string;
  company: string;
  title: string;
  startDate: string;
  endDate: string;
  location: string;
  bullets: BulletItem[];
}

export interface EducationEntry {
  id: string;
  // ... details: BulletItem[];
}

export interface SkillCategory {
  id: string;
  category: string;
  skills: SkillItem[];
}

export interface Project {
  id: string;
  name: string;
  year: string;
  description: string;
  technologies?: string;
  bullets?: BulletItem[];
}

export interface Award {
  id: string;
  year: string;
  title: string;
  description?: string;
}

export interface AdditionalEntry {
  id: string;
  // ... bullets: BulletItem[];
}

export interface AdditionalSection {
  id: string;
  title: string;
  entries: AdditionalEntry[];
}

export interface PersonalInfo {
  // ... links: Array<{ id: string; label: string; url: string }>;
}
```

From frontend/src/utils/idHelpers.ts:
```typescript
export function generateId(): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update useFormBuilder factories + CRUD + formDataPatch</name>
  <files>frontend/src/hooks/useFormBuilder.ts, frontend/src/utils/formDataPatch.ts</files>
  <read_first>
    - frontend/src/hooks/useFormBuilder.ts (full file — all factory functions and CRUD operations)
    - frontend/src/utils/formDataPatch.ts (full file — setAtPath and applyTailorChanges)
    - frontend/src/types/index.ts (updated types from Plan 01 — BulletItem, SkillItem)
    - frontend/src/utils/idHelpers.ts (generateId function from Plan 01)
  </read_first>
  <action>
**A. Update `frontend/src/hooks/useFormBuilder.ts`:**

Add imports at top:
```typescript
import { generateId } from '../utils/idHelpers';
import type { BulletItem, SkillItem } from '../types';
```
(Add BulletItem and SkillItem to the existing type import from '../types')

Update ALL factory functions to include `id: generateId()`:

```typescript
function emptyBullet(): BulletItem {
  return { id: generateId(), text: '' };
}

function emptySkillItem(): SkillItem {
  return { id: generateId(), text: '' };
}

function emptyPersonalInfo(): PersonalInfo {
  return {
    fullName: '', email: '', phone: '', location: '',
    links: [],
    summary: '', personalOrder: [...DEFAULT_PERSONAL_ORDER],
  };
}

function emptyWorkEntry(): WorkEntry {
  return { id: generateId(), company: '', title: '', startDate: '', endDate: '', location: '', bullets: [emptyBullet()] };
}

function emptyEducationEntry(): EducationEntry {
  return { id: generateId(), school: '', degree: '', startDate: '', endDate: '', location: '', gpa: '', details: [] };
}

function emptySkillCategory(): SkillCategory {
  return { id: generateId(), category: '', skills: [] };
}

function emptyProject(): Project {
  return { id: generateId(), name: '', year: '', description: '', technologies: '', bullets: [] };
}

function emptyAward(): Award {
  return { id: generateId(), year: '', title: '', description: '' };
}

function emptyAdditionalEntry(): AdditionalEntry {
  return { id: generateId(), title: '', subtitle: '', startDate: '', endDate: '', location: '', description: '', bullets: [emptyBullet()] };
}

function emptyAdditionalSection(index: number): AdditionalSection {
  return { id: generateId(), title: `Additional Section ${index + 1}`, entries: [emptyAdditionalEntry()] };
}
```

Update CRUD operations that deal with bullets/skills:

1. `addBullet` (line ~203): change `[...e.bullets, '']` to `[...e.bullets, emptyBullet()]`

2. `updateBullet` (line ~212): change the map callback from `bi === bulletIndex ? value : b` to `bi === bulletIndex ? { ...b, text: value } : b`. This preserves the bullet's ID while updating only the text.

3. `addEduDetail` (line ~262): change `[...e.details, '']` to `[...e.details, emptyBullet()]`

4. `updateEduDetail` (line ~271): change from `di === detailIndex ? value : d` to `di === detailIndex ? { ...d, text: value } : d`

5. `updateSkillsText` (line ~321): This is the comma-split function. Currently: `const skills = value.split(',').map(s => s.trim()).filter(Boolean)`. Change to preserve IDs for existing skills:
```typescript
const updateSkillsText = useCallback((index: number, value: string) => {
  const newTexts = value.split(',').map(s => s.trim()).filter(Boolean);
  setFormData(prev => ({
    ...prev,
    skills: prev.skills.map((s, i) => {
      if (i !== index) return s;
      // Preserve IDs for skills whose text hasn't changed
      const updatedSkills: SkillItem[] = newTexts.map((text, ti) => {
        const existing = s.skills[ti];
        if (existing && existing.text === text) return existing;
        // Try to find by text match (handles reordering from comma edits)
        const matchByText = s.skills.find(sk => sk.text === text);
        if (matchByText) return matchByText;
        return { id: generateId(), text };
      });
      return { ...s, skills: updatedSkills };
    }),
  }));
}, []);
```

6. `addLink` (line ~161): change `{ label: '', url: '' }` to `{ id: generateId(), label: '', url: '' }`

7. `addProjectBullet` (~line 350): change `[...(p.bullets || []), '']` to `[...(p.bullets || []), emptyBullet()]`

8. `updateProjectBullet` (~line 358): change the value replacement to `{ ...b, text: value }` pattern (preserve ID, update text)

9. Find `addAdditionalBullet` and `updateAdditionalBullet` in the additional sections CRUD section (~line 440+). Apply the same pattern: add uses `emptyBullet()`, update uses `{ ...b, text: value }`.

10. `removeProjectBullet` and `removeAdditionalBullet` — these filter by index, which still works with BulletItem arrays. No change needed.

11. `reorderBullets`, `reorderEduDetails` — these use the `reorder()` helper which moves array items. Works with BulletItem arrays unchanged.

**B. Update `frontend/src/utils/formDataPatch.ts`:**

Add import: `import type { BulletItem, SkillItem } from '../types';`
Add import: `import { generateId } from './idHelpers';`

The core issue: when `applyTailorChanges` calls `setAtPath(patched, change.fieldPath, alt.value)`, and the fieldPath resolves to a BulletItem (e.g., `workExperience[0].bullets[2]`), the AI's `alt.value` is a string, but the target is a BulletItem object. We need to wrap the string value in a BulletItem, preserving the existing ID.

Modify `setAtPath` to handle this:

```typescript
function setAtPath(obj: Record<string, unknown>, path: string, value: unknown): void {
  const segs = parsePath(path);
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  let cur: any = obj;
  for (let i = 0; i < segs.length - 1; i++) {
    if (cur[segs[i]] == null) {
      cur[segs[i]] = typeof segs[i + 1] === 'number' ? [] : {};
    }
    cur = cur[segs[i]];
  }
  const lastSeg = segs[segs.length - 1];
  const existing = cur[lastSeg];

  // If the existing value is a BulletItem/SkillItem and the new value is a string,
  // preserve the ID and update only the text field
  if (existing && typeof existing === 'object' && 'text' in existing && 'id' in existing && typeof value === 'string') {
    cur[lastSeg] = { ...existing, text: value };
  } else if (typeof value === 'string' && _isBulletArray(cur, segs, path)) {
    // Adding a new bullet to a BulletItem array — wrap in BulletItem
    cur[lastSeg] = { id: generateId(), text: value };
  } else {
    cur[lastSeg] = value;
  }
}
```

Add a helper to detect if the parent container holds BulletItem objects:
```typescript
function _isBulletArray(container: unknown, segs: (string | number)[], path: string): boolean {
  // Check if the path ends with a bullet/detail/skill index
  const bulletPaths = /\.(bullets|details)\[\d+\]$/;
  const skillPaths = /\.skills\[\d+\]$/;
  return bulletPaths.test(path) || skillPaths.test(path);
}
```

Actually, a simpler approach: check if the container is an array and its first element (if any) has `text` and `id` fields:

```typescript
function _isStructuredArray(arr: unknown[]): boolean {
  if (arr.length === 0) return false;
  const first = arr[0];
  return typeof first === 'object' && first !== null && 'text' in first && 'id' in first;
}
```

Then in `setAtPath`, after computing the final assignment:
```typescript
  const lastSeg = segs[segs.length - 1];
  const existing = cur[lastSeg];

  // Handle BulletItem/SkillItem: preserve ID, update text
  if (existing && typeof existing === 'object' && 'text' in existing && 'id' in existing && typeof value === 'string') {
    cur[lastSeg] = { ...existing, text: value };
  } else if (typeof lastSeg === 'number' && Array.isArray(cur) && typeof value === 'string' && _isStructuredArray(cur)) {
    // Inserting into a structured array — wrap value
    cur[lastSeg] = { id: generateId(), text: value };
  } else {
    cur[lastSeg] = value;
  }
```

For the `changeType: 'add'` case in `applyTailorChanges`: when the change type is `add` and the path points to a bullet array (e.g., `workExperience[0].bullets[3]` where index 3 doesn't exist), `setAtPath` already handles creating intermediate arrays. The new code above handles wrapping the value in BulletItem when inserting into a structured array. This should work.

Also handle the case where `alt.value` is a `string[]` (for skill arrays): the existing `setAtPath` sets the entire array. If the target is a SkillItem array, the string array values need to be wrapped:

In `applyTailorChanges`, after getting `alt.value`, add wrapping logic:
```typescript
    try {
      if (Array.isArray(alt.value)) {
        // Check if target array holds structured items
        const segs = parsePath(change.fieldPath);
        let target: any = patched;
        for (const seg of segs) {
          if (target == null) break;
          target = (target as any)[seg];
        }
        if (Array.isArray(target) && target.length > 0 && typeof target[0] === 'object' && 'text' in target[0]) {
          // Wrap string[] values as SkillItem[] / BulletItem[], preserving existing IDs where possible
          const wrapped = (alt.value as string[]).map((v, idx) => {
            const existing = target[idx];
            if (existing && typeof existing === 'object' && existing.text === v) return existing;
            return { id: generateId(), text: v };
          });
          setAtPath(patched as Record<string, unknown>, change.fieldPath, wrapped);
        } else {
          setAtPath(patched as Record<string, unknown>, change.fieldPath, alt.value);
        }
      } else {
        setAtPath(patched as Record<string, unknown>, change.fieldPath, alt.value);
      }
    } catch {
      // Skip unresolvable paths
    }
```

Wait, this is getting complex. Let me simplify. The `alt.value` for bullets is always a single string (modifying one bullet). The `alt.value` for skills is a `string[]` (replacing the entire skills array). For `changeType: 'modify'` on a bullet, the path is `workExperience[0].bullets[2]` and value is a string — the `setAtPath` BulletItem detection handles this. For `changeType: 'modify'` on skills, the path is `skills[0].skills` and value is `["Python", "Go"]` — we need to wrap. For `changeType: 'add'` on a bullet, the path is `workExperience[0].bullets[3]` — the array insertion wrapping handles this.

Simplify `applyTailorChanges` to handle the `string[]` → `SkillItem[]` case directly in the function, not in `setAtPath`:

Leave `setAtPath` with the BulletItem single-value wrapping logic. Add to `applyTailorChanges` before the `setAtPath` call:

```typescript
    let valueToSet = alt.value;

    // If value is string[] and target is a structured array (SkillItem[]/BulletItem[]),
    // wrap each string in a structured object
    if (Array.isArray(valueToSet) && valueToSet.every(v => typeof v === 'string')) {
      // Navigate to the target to check its current type
      const segs = parsePath(change.fieldPath);
      let target: unknown = patched;
      for (const seg of segs) {
        if (target == null) break;
        target = (target as Record<string, unknown>)[seg as string];
      }
      if (Array.isArray(target) && target.length > 0 && typeof target[0] === 'object' && target[0] !== null && 'text' in (target[0] as Record<string, unknown>)) {
        valueToSet = (valueToSet as string[]).map((text) => ({ id: generateId(), text }));
      }
    }

    try {
      setAtPath(patched as Record<string, unknown>, change.fieldPath, valueToSet);
    } catch {
      // Skip unresolvable paths
    }
```
  </action>
  <verify>
    <automated>cd /Users/yagotobi/Documents/Code/GitHub/Projects/CV-Maker/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/hooks/useFormBuilder.ts imports `generateId` from `../utils/idHelpers`
    - frontend/src/hooks/useFormBuilder.ts imports `BulletItem` and `SkillItem` from `../types`
    - frontend/src/hooks/useFormBuilder.ts `emptyWorkEntry()` returns object with `id: generateId()` and `bullets: [emptyBullet()]`
    - frontend/src/hooks/useFormBuilder.ts `emptyBullet()` returns `{ id: generateId(), text: '' }`
    - frontend/src/hooks/useFormBuilder.ts `addBullet` pushes `emptyBullet()` not bare `''`
    - frontend/src/hooks/useFormBuilder.ts `updateBullet` updates `{ ...b, text: value }` not replacing with bare string
    - frontend/src/hooks/useFormBuilder.ts `updateSkillsText` produces `SkillItem[]` preserving IDs for unchanged skills
    - frontend/src/hooks/useFormBuilder.ts `addLink` includes `id: generateId()` in the new link object
    - frontend/src/utils/formDataPatch.ts imports `BulletItem` type and `generateId`
    - frontend/src/utils/formDataPatch.ts `setAtPath` detects BulletItem at leaf and preserves ID
    - `npx tsc --noEmit` exits with 0 errors
  </acceptance_criteria>
  <done>All factory functions generate IDs. Bullet/skill CRUD preserves ID stability. formDataPatch wraps string values in BulletItem when target is structured. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: UI sections + frontend tests</name>
  <files>frontend/src/features/form-builder/sections/WorkSection.tsx, frontend/src/features/form-builder/sections/EducationSection.tsx, frontend/src/features/form-builder/sections/SkillsSection.tsx, frontend/src/features/form-builder/sections/ProjectsSection.tsx, frontend/src/features/form-builder/sections/AdditionalSectionView.tsx, frontend/src/__tests__/useFormBuilder.test.ts, frontend/src/__tests__/formDataPatch.test.ts</files>
  <read_first>
    - frontend/src/features/form-builder/sections/WorkSection.tsx (bullet rendering and onChange)
    - frontend/src/features/form-builder/sections/EducationSection.tsx (details rendering)
    - frontend/src/features/form-builder/sections/SkillsSection.tsx (skills.join rendering)
    - frontend/src/features/form-builder/sections/ProjectsSection.tsx (bullet rendering)
    - frontend/src/features/form-builder/sections/AdditionalSectionView.tsx (bullet rendering)
    - frontend/src/__tests__/useFormBuilder.test.ts (existing test expectations to update)
    - frontend/src/utils/formDataPatch.ts (updated setAtPath from Task 1)
    - frontend/src/types/index.ts (BulletItem/SkillItem types from Plan 01)
  </read_first>
  <action>
**A. Update UI sections to access `.text` on bullets/skills:**

In each section component, bullets are currently rendered as bare strings. After the type change, they are `BulletItem` objects. The key pattern change is:

- Where `bullet` is used as a string value (in `<input value={bullet}>` or `<textarea value={bullet}>`), change to `bullet.text`
- Where `bullet` is used as a React key, change from `key={bi}` (index) to `key={bullet.id}` (stable ID)
- Same pattern for education `detail` -> `detail.text`, project bullets, additional bullets

**WorkSection.tsx:**
- Find `{job.bullets.map((bullet, bi) => (` — change the rendered value from `value={bullet}` to `value={bullet.text}`
- Change React key from index-based `key={bi}` to `key={bullet.id}`
- The onChange handler calls `updateBullet(ji, bi, e.target.value)` which is already updated in useFormBuilder to handle `{ ...b, text: value }`. No change needed on the onChange call.

**EducationSection.tsx:**
- Find `details` mapping — change `value={detail}` to `value={detail.text}`
- Change React key to `key={detail.id}`

**SkillsSection.tsx:**
- Find `cat.skills.join(", ")` — change to `cat.skills.map(s => s.text).join(", ")`
- The onChange for skills textarea calls `updateSkillsText(i, e.target.value)` which already handles SkillItem conversion (updated in Task 1). No change needed on the onChange call.

**ProjectsSection.tsx:**
- Find `{proj.bullets.map((bullet, bi) => (` — change `value={bullet}` to `value={bullet.text}`
- Change React key to `key={bullet.id}`

**AdditionalSectionView.tsx:**
- Find `{entry.bullets.map((bullet, bi) => (` — change `value={bullet}` to `value={bullet.text}`
- Change React key to `key={bullet.id}`

**B. Create `frontend/src/__tests__/formDataPatch.test.ts`:**

```typescript
/**
 * Tests for formDataPatch utility — verifying path resolution
 * works with BulletItem/SkillItem structured types.
 *
 * Covers DATA-02: tailor path resolution with structured types.
 */
import { describe, it, expect } from 'vitest';
import { applyTailorChanges } from '../utils/formDataPatch';
import type { CVFormData, TailorChange } from '../types';
import { generateId } from '../utils/idHelpers';

function makeTestFormData(): CVFormData {
  return {
    templateId: 'med-length-proff-cv',
    personalInfo: {
      fullName: 'John Doe', email: '', phone: '', location: '',
      links: [{ id: 'link-1', label: 'GitHub', url: 'https://github.com' }],
    },
    workExperience: [{
      id: 'work-1',
      company: 'Acme',
      title: 'Engineer',
      startDate: 'Jan 2020',
      endDate: 'Present',
      location: 'NYC',
      bullets: [
        { id: 'b1', text: 'Built X' },
        { id: 'b2', text: 'Led Y' },
        { id: 'b3', text: 'Managed Z' },
      ],
    }],
    education: [{
      id: 'edu-1',
      school: 'MIT',
      degree: 'BS CS',
      startDate: '2016',
      endDate: '2020',
      location: 'MA',
      details: [{ id: 'd1', text: 'GPA 4.0' }],
    }],
    skills: [{
      id: 'skill-1',
      category: 'Languages',
      skills: [
        { id: 'sk1', text: 'Python' },
        { id: 'sk2', text: 'TypeScript' },
      ],
    }],
  };
}

describe('applyTailorChanges with BulletItem', () => {
  it('modifies bullet text while preserving ID', () => {
    const fd = makeTestFormData();
    const changes: TailorChange[] = [{
      id: 'c1',
      fieldPath: 'workExperience[0].bullets[1]',
      section: 'Work Experience',
      description: 'Enhanced bullet',
      currentValue: 'Led Y',
      alternatives: [{ label: 'Suggested', value: 'Led Y initiative achieving 50% improvement' }],
      changeType: 'modify',
    }];
    const result = applyTailorChanges(fd, changes, new Set(['c1']));
    // Text should be updated
    expect(result.workExperience[0].bullets[1].text).toBe('Led Y initiative achieving 50% improvement');
    // ID should be preserved
    expect(result.workExperience[0].bullets[1].id).toBe('b2');
    // Other bullets unchanged
    expect(result.workExperience[0].bullets[0].text).toBe('Built X');
    expect(result.workExperience[0].bullets[0].id).toBe('b1');
  });

  it('modifies education detail while preserving ID', () => {
    const fd = makeTestFormData();
    const changes: TailorChange[] = [{
      id: 'c2',
      fieldPath: 'education[0].details[0]',
      section: 'Education',
      description: 'Enhanced detail',
      currentValue: 'GPA 4.0',
      alternatives: [{ label: 'Suggested', value: 'Graduated Magna Cum Laude, GPA 4.0' }],
      changeType: 'modify',
    }];
    const result = applyTailorChanges(fd, changes, new Set(['c2']));
    expect(result.education[0].details[0].text).toBe('Graduated Magna Cum Laude, GPA 4.0');
    expect(result.education[0].details[0].id).toBe('d1');
  });

  it('handles non-selected changes (skips them)', () => {
    const fd = makeTestFormData();
    const changes: TailorChange[] = [{
      id: 'c3',
      fieldPath: 'workExperience[0].bullets[0]',
      section: 'Work',
      description: 'Change',
      currentValue: 'Built X',
      alternatives: [{ label: 'Suggested', value: 'Redesigned X' }],
      changeType: 'modify',
    }];
    const result = applyTailorChanges(fd, changes, new Set([])); // not selected
    expect(result.workExperience[0].bullets[0].text).toBe('Built X');
  });

  it('modifies plain string field normally', () => {
    const fd = makeTestFormData();
    const changes: TailorChange[] = [{
      id: 'c4',
      fieldPath: 'workExperience[0].title',
      section: 'Work',
      description: 'Updated title',
      currentValue: 'Engineer',
      alternatives: [{ label: 'Suggested', value: 'Senior Software Engineer' }],
      changeType: 'modify',
    }];
    const result = applyTailorChanges(fd, changes, new Set(['c4']));
    expect(result.workExperience[0].title).toBe('Senior Software Engineer');
  });
});
```

**C. Update `frontend/src/__tests__/useFormBuilder.test.ts`:**

The existing tests check for bare string bullets like `expect(fd.workExperience[0].bullets).toEqual([''])`. After the type change, bullets are `BulletItem[]`.

Update expectations:
- `expect(fd.workExperience[0].bullets).toEqual([''])` → `expect(fd.workExperience[0].bullets).toHaveLength(1)` and `expect(fd.workExperience[0].bullets[0].text).toBe('')` and `expect(fd.workExperience[0].bullets[0].id).toBeDefined()`
- `expect(fd.skills[0].skills).toEqual([])` → stays the same (empty array is still empty array)
- Tests that check `fd.personalInfo.links` empty array → stays the same
- If there are tests checking imported data with bare string bullets, update the imported data fixtures to use `BulletItem` format OR verify they still work via the data pipeline (AI sends bare strings, formDataPatch wraps them)

Also update the `importedData` fixture used in the "with importedData" tests. The imported data should use the new format:
- `bullets: [{ id: 'imp-b1', text: 'Built something' }]` instead of `bullets: ['Built something']`
- `details: [{ id: 'imp-d1', text: 'GPA 3.9' }]`
- `skills: [{ id: 'imp-sk1', text: 'React' }]`
- Add `id` fields to all entries in the fixture

Check if the test has assertions about `addBullet`, `updateBullet`, etc. Update those assertions to expect BulletItem structures.

Mock `generateId` in tests for deterministic IDs:
```typescript
vi.mock('../utils/idHelpers', () => ({
  generateId: vi.fn(() => `test-id-${Math.random().toString(36).slice(2, 8)}`),
}));
```

Or alternatively, just assert `.toBeDefined()` on id fields rather than exact values.
  </action>
  <verify>
    <automated>cd /Users/yagotobi/Documents/Code/GitHub/Projects/CV-Maker/frontend && npx tsc --noEmit && npx vitest run --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - WorkSection.tsx renders `bullet.text` in input value, uses `bullet.id` as React key
    - EducationSection.tsx renders `detail.text` in input value, uses `detail.id` as React key
    - SkillsSection.tsx renders `cat.skills.map(s => s.text).join(", ")` for display
    - ProjectsSection.tsx renders `bullet.text` in input value, uses `bullet.id` as React key
    - AdditionalSectionView.tsx renders `bullet.text` in input value, uses `bullet.id` as React key
    - frontend/src/__tests__/formDataPatch.test.ts exists with tests for BulletItem leaf handling
    - frontend/src/__tests__/formDataPatch.test.ts tests pass (`npx vitest run src/__tests__/formDataPatch.test.ts` exits 0)
    - frontend/src/__tests__/useFormBuilder.test.ts updated expectations match BulletItem/SkillItem structures
    - `npx tsc --noEmit` exits 0 (zero TypeScript errors)
    - `npx vitest run` all tests pass
  </acceptance_criteria>
  <done>All UI sections render .text from BulletItem/SkillItem. Stable IDs used as React keys. formDataPatch test proves tailor changes work with structured types. useFormBuilder test updated for new data shapes. TypeScript compiles clean, all tests green.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` — zero errors (the definitive check that all types are consistent)
- `cd frontend && npx vitest run --reporter=verbose` — all frontend tests pass
- `cd frontend && npx vitest run src/__tests__/formDataPatch.test.ts` — new test suite passes
- `cd frontend && npx vitest run src/__tests__/useFormBuilder.test.ts` — updated test suite passes
</verification>

<success_criteria>
- All frontend consumers handle ID-bearing CVFormData correctly
- Factory functions produce entries with unique IDs
- Bullet/skill CRUD operations preserve ID stability
- UI sections display .text from structured types (not [object Object])
- formDataPatch correctly wraps string values as BulletItem when modifying structured leaves
- TypeScript compiles with zero errors
- All frontend tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-model-prep/01-03-SUMMARY.md`
</output>
