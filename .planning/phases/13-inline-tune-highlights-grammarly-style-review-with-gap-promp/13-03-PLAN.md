---
phase: 13
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/src/features/direct-edit/components/ChangePopover.tsx
  - frontend/src/features/direct-edit/components/ChangePopover.module.css
  - frontend/src/features/direct-edit/components/ChangeHighlight.tsx
  - frontend/src/features/direct-edit/components/ChangeHighlight.module.css
  - frontend/src/features/direct-edit/components/GapPromptChips.tsx
  - frontend/src/features/direct-edit/components/GapPromptChips.module.css
autonomous: true
requirements:
  - D-04
  - D-05
  - D-06
  - D-13
  - D-14
  - D-17
  - D-18
  - D-19
user_setup: []

must_haves:
  truths:
    - "@floating-ui/react@^0.27.19 is installed and resolvable from frontend/"
    - "ChangePopover renders a compact body for minor severity and an expanded body (BEFORE/AFTER labels) for strong severity per D-18"
    - "ChangePopover supports keyboard nav: ArrowRight/Left advance, Enter accepts, Escape closes per D-19"
    - "ChangePopover virtual-element anchoring positions next to the active highlight without clipping at viewport edges per RESEARCH §Pattern 1"
    - "ChangeHighlight is a presentational wrapper that renders <span> with severity squiggle classes per D-13/D-14"
    - "GapPromptChips renders one chip per missing[] entry, click reveals an inline input per D-04/D-05/D-06"
    - "GapPromptChips emits onClarificationsChange with only the typed strings (untouched chips excluded)"
  artifacts:
    - path: "frontend/src/features/direct-edit/components/ChangePopover.tsx"
      provides: "Floating popover with compact + expanded variants and keyboard nav"
      exports: ["ChangePopover"]
      contains: "useFloating"
    - path: "frontend/src/features/direct-edit/components/ChangePopover.module.css"
      provides: "Popover surface, compact/expanded layout, severity-tier accents"
    - path: "frontend/src/features/direct-edit/components/ChangeHighlight.tsx"
      provides: "Presentational squiggle wrapper for inline ranges"
      exports: ["ChangeHighlight"]
    - path: "frontend/src/features/direct-edit/components/ChangeHighlight.module.css"
      provides: "Wavy underline + severity-tier color tokens shared with EditableField"
    - path: "frontend/src/features/direct-edit/components/GapPromptChips.tsx"
      provides: "Click-to-expand chip array for match-analysis missing[]"
      exports: ["GapPromptChips"]
      contains: "onClarificationsChange"
    - path: "frontend/src/features/direct-edit/components/GapPromptChips.module.css"
      provides: "Chip + inline-textarea styling matching UI-SPEC"
  key_links:
    - from: "frontend/src/features/direct-edit/components/ChangePopover.tsx"
      to: "@floating-ui/react"
      via: "useFloating + autoUpdate + flip + shift + offset middleware"
      pattern: "useFloating\\("
    - from: "frontend/src/features/direct-edit/components/ChangePopover.tsx"
      to: "frontend/src/features/direct-edit/utils/severity.ts"
      via: "Severity union determines compact vs expanded layout"
      pattern: "Severity"
    - from: "frontend/src/features/direct-edit/components/ChangePopover.tsx"
      to: "@floating-ui/react FloatingFocusManager"
      via: "Wraps popover content for focus trap and Escape close"
      pattern: "FloatingFocusManager"
    - from: "frontend/src/features/direct-edit/components/GapPromptChips.tsx"
      to: "TuneRail (Plan 04)"
      via: "onClarificationsChange callback feeds tuneRail.userClarifications state"
      pattern: "onClarificationsChange"
---

<objective>
Build the three remaining presentational primitives that the rail (Plan 04) wires together: `ChangePopover` (Grammarly-style floating card with compact + expanded variants and full keyboard nav), `ChangeHighlight` (squiggle wrapper used by the popover anchor and downstream consumers), and `GapPromptChips` (click-to-expand chip array seeded from match-analysis `missing[]`). Also installs the Wave 0 prerequisite `@floating-ui/react@^0.27.19`.

Purpose: Plan 04's TuneRail composes all three of these. They are independent of each other in implementation but share styling tokens (severity tiers + UI-SPEC color palette) and the `Severity` type from Plan 02. Splitting them out keeps Plan 04's surface focused on rail state and orchestration.

Output: One dependency install, three new components (each with co-located CSS module), and three Wave 0 scaffolds (ChangePopover.test.tsx + GapPromptChips.test.tsx from Plan 01) turn green. ChangeHighlight ships with new tests authored in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-inline-tune-highlights-grammarly-style-review-with-gap-promp/13-CONTEXT.md
@.planning/phases/13-inline-tune-highlights-grammarly-style-review-with-gap-promp/13-RESEARCH.md
@.planning/phases/13-inline-tune-highlights-grammarly-style-review-with-gap-promp/13-PATTERNS.md
@.planning/phases/13-inline-tune-highlights-grammarly-style-review-with-gap-promp/13-UI-SPEC.md
@.planning/phases/13-inline-tune-highlights-grammarly-style-review-with-gap-promp/13-01-SUMMARY.md
@.planning/phases/13-inline-tune-highlights-grammarly-style-review-with-gap-promp/13-02-SUMMARY.md
@frontend/src/features/direct-edit/components/FloatingFormatToolbar.tsx
@frontend/src/features/direct-edit/components/TunePanel.tsx
@frontend/src/features/direct-edit/components/ChangeCard.tsx
@frontend/src/styles/variables.css
@frontend/package.json

<interfaces>
<!-- Types and exports the executor must produce. Severity comes from Plan 02. -->

EXISTING (from Plan 02) — frontend/src/features/direct-edit/utils/severity.ts:
```typescript
export type Severity = 'strong' | 'minor' | 'add' | 'delete';
export function inferSeverity(change: TailorChange): Severity;
```

EXISTING (from frontend/src/types/index.ts):
```typescript
export interface TailorChange {
  id: string;
  fieldPath: string;
  changeType: 'modify' | 'add' | 'remove';
  currentValue: unknown;
  alternatives: TailorAlternative[];
  description: string;
  section: string;
}
```

NEW — frontend/src/features/direct-edit/components/ChangePopover.tsx:
```typescript
import type { TailorChange } from '../../../types';
import type { Severity } from '../utils/severity';

export interface ChangePopoverProps {
  activeChange: TailorChange | null;
  severity: Severity | null;
  anchorRange: Range | null;            // virtual-element anchor
  onAccept: (changeId: string) => void;
  onSkip: (changeId: string) => void;
  onAdvance: (direction: 'next' | 'prev') => void;
  onClose: () => void;
}

export function ChangePopover(props: ChangePopoverProps): JSX.Element | null;
```

NEW — frontend/src/features/direct-edit/components/ChangeHighlight.tsx:
```typescript
import type { Severity } from '../utils/severity';

export interface ChangeHighlightProps {
  changeId: string;
  severity: Severity;
  isActive: boolean;
  children: React.ReactNode;
  onClick?: (changeId: string) => void;
}

export function ChangeHighlight(props: ChangeHighlightProps): JSX.Element;
```

NEW — frontend/src/features/direct-edit/components/GapPromptChips.tsx:
```typescript
export interface GapPromptChipsProps {
  missing: string[];                                // from MatchAnalysis.missing
  onClarificationsChange: (clarifications: string[]) => void;
}

export function GapPromptChips(props: GapPromptChipsProps): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @floating-ui/react + ChangeHighlight wrapper</name>
  <files>frontend/package.json, frontend/package-lock.json, frontend/src/features/direct-edit/components/ChangeHighlight.tsx, frontend/src/features/direct-edit/components/ChangeHighlight.module.css, frontend/src/__tests__/ChangeHighlight.test.tsx</files>
  <read_first>
    - frontend/package.json (verify floating-ui is NOT already present; identify peer deps section)
    - frontend/src/features/direct-edit/components/EditableField.module.css (Plan 02 already added .highlight + .tier* rules — read them so ChangeHighlight.module.css does NOT duplicate but composes via shared variables)
    - frontend/src/styles/variables.css (existing tokens: --error, --error-light, --accent, --accent-light, --success, --success-light, --radius-sm)
    - .planning/phases/13-inline-tune-highlights-grammarly-style-review-with-gap-promp/13-RESEARCH.md (lines 280-311 Pattern 2 highlight overlay rule; line 893+ floating-ui version)
    - .planning/phases/13-inline-tune-highlights-grammarly-style-review-with-gap-promp/13-PATTERNS.md (lines 256-300 ChangeHighlight pattern)
    - .planning/phases/13-inline-tune-highlights-grammarly-style-review-with-gap-promp/13-UI-SPEC.md (severity-tier color table lines 113-119)
    - frontend/src/__tests__/EditableField.test.tsx (test scaffold pattern)
  </read_first>
  <behavior>
    Install:
    - `npm install @floating-ui/react@^0.27.19 --save` from `frontend/` produces a clean install (no peer-dep warnings) and updates both `package.json` and `package-lock.json`.
    - Post-install, `node -e "require('@floating-ui/react')"` exits 0 from `frontend/`.

    ChangeHighlight component:
    - Renders `<span class={...} data-change-id={changeId} data-severity={severity}>{children}</span>`.
    - Class composition: `styles.highlight ${styles[`tier${capitalizedSeverity}`]} ${isActive ? styles.active : ''}`.
    - Calls `onClick(changeId)` when clicked (not when child elements are clicked separately — use stopPropagation only on the wrapper itself).
    - Pure presentational — no state, no effects.

    Tests (frontend/src/__tests__/ChangeHighlight.test.tsx):
    - Renders span with data-change-id and data-severity attrs.
    - Applies `.tierStrong` class for severity='strong'.
    - Applies `.active` class only when isActive=true.
    - Click triggers onClick with the changeId.
    - Renders children inside the span.
  </behavior>
  <action>
    1. Install dependency from `frontend/`:
       ```
       cd frontend && npm install @floating-ui/react@^0.27.19 --save
       ```
       Verify version pinned to `^0.27.19` in `package.json` dependencies block. If npm picks a newer minor (e.g., 0.28.x) due to existing react@19 peer constraints, document the chosen version in the SUMMARY but DO NOT downgrade — RESEARCH.md confirmed 0.27.19 is the floor.

    2. `frontend/src/features/direct-edit/components/ChangeHighlight.tsx` (NEW):
       - Functional component, named export, `export function ChangeHighlight(...)`.
       - Use `import type { Severity }` (verbatimModuleSyntax compliance).
       - Capitalize first letter of severity for the class lookup: `const tierClass = styles[`tier${severity[0].toUpperCase() + severity.slice(1)}`]`.
       - `onClick` handler MUST guard against undefined: `onClick?.(changeId)`.
       - DO NOT compute layout offsets here; the popover anchors via DOM Range from `useChangeHighlights.getRangeForChange`. ChangeHighlight is purely visual.

    3. `frontend/src/features/direct-edit/components/ChangeHighlight.module.css` (NEW):
       Copy the same `.highlight`, `.tierStrong`, `.tierMinor`, `.tierAdd`, `.tierDelete`, `.active.tierStrong` (and the three parallels) rules that Plan 02 added to `EditableField.module.css`. The duplication is intentional — this component is consumed in contexts where the EditableField CSS scope does not apply (e.g., wrapped around plain text in MedLengthTemplate). Use the SAME color tokens (`--error`, `--accent`, `--success`, plus `-light` variants) so the visual is identical.
       - Class names: `.highlight`, `.tierStrong`, `.tierMinor`, `.tierAdd`, `.tierDelete`, `.active`.
       - Wavy underline: `text-decoration-line: underline; text-decoration-style: wavy; text-decoration-thickness: 2px; text-underline-offset: 3px; text-decoration-skip-ink: none;`.
       - Active background: `.active.tierStrong { background-color: var(--error-light); }` — repeat for each tier.
       - Cursor: `cursor: pointer;` on `.highlight`.
       - Smooth color transition: `transition: background-color 0.15s ease;`.

    4. `frontend/src/__tests__/ChangeHighlight.test.tsx` (NEW):
       - Use the canonical Vitest + RTL pattern from `frontend/src/__tests__/EditableField.test.tsx`.
       - Top-of-file analog comment: `// Analog: frontend/src/__tests__/EditableField.test.tsx (RTL render + class assertion pattern)`.
       - Tests as listed in <behavior>.
       - Click test uses `fireEvent.click(screen.getByText('hello world').closest('span'))` and asserts onClick called with the changeId arg.

    Reference:
    - PATTERNS.md lines 256-300 (ChangeHighlight pattern)
    - UI-SPEC §Color severity-tier table
    - RESEARCH.md Pattern 2 (highlight overlay rule)
    - D-13, D-14 in CONTEXT.md
  </action>
  <verify>
    <automated>cd frontend && npm ls @floating-ui/react && npx vitest run src/__tests__/ChangeHighlight.test.tsx --reporter=basic && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm ls @floating-ui/react` exits 0 with version `^0.27.19` (or newer 0.27.x / 0.28.x) resolved.
    - `grep -n "@floating-ui/react" frontend/package.json` returns ≥1 match in the dependencies section.
    - `cd frontend && npx vitest run src/__tests__/ChangeHighlight.test.tsx --reporter=basic` exits 0 (all 5 tests pass).
    - `cd frontend && npx tsc --noEmit` exits 0.
    - `grep -nE "data-change-id|data-severity" frontend/src/features/direct-edit/components/ChangeHighlight.tsx` returns ≥2 matches.
    - `grep -v '^/\*\|^\s\*' frontend/src/features/direct-edit/components/ChangeHighlight.module.css | grep -c "tierStrong\|tierMinor\|tierAdd\|tierDelete"` returns ≥4.
    - `grep -nE "text-decoration-style: wavy" frontend/src/features/direct-edit/components/ChangeHighlight.module.css` returns ≥1 match.
    - `grep -n "import type" frontend/src/features/direct-edit/components/ChangeHighlight.tsx` returns ≥1 match (Severity import is type-only).
  </acceptance_criteria>
  <done>
    `@floating-ui/react@^0.27.19` installed; `ChangeHighlight` component + CSS module + tests committed; tsc clean. Plan 04 can now wrap MedLengthTemplate text in `<ChangeHighlight>` for inline highlights that don't live inside an EditableField.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ChangePopover with @floating-ui/react + keyboard nav</name>
  <files>frontend/src/features/direct-edit/components/ChangePopover.tsx, frontend/src/features/direct-edit/components/ChangePopover.module.css</files>
  <read_first>
    - frontend/src/features/direct-edit/components/FloatingFormatToolbar.tsx (FULL file — current hand-rolled positioning logic; the new popover REPLACES this approach with @floating-ui/react)
    - frontend/src/features/direct-edit/components/ChangeCard.tsx (FULL file — accept/skip/diff visual layout to mimic in expanded variant lines 73-148)
    - frontend/src/__tests__/ChangePopover.test.tsx (Plan 01 scaffold — exact assertions this component must satisfy)
    - .planning/phases/13-inline-tune-highlights-grammarly-style-review-with-gap-promp/13-RESEARCH.md (lines 230-279 Pattern 1 floating-ui virtual element + offset/flip/shift; lines 658-744 Code Examples Example 3 ChangePopover skeleton)
    - .planning/phases/13-inline-tune-highlights-grammarly-style-review-with-gap-promp/13-PATTERNS.md (lines 301-376 ChangePopover pattern + variants table)
    - .planning/phases/13-inline-tune-highlights-grammarly-style-review-with-gap-promp/13-UI-SPEC.md (lines 125-180 popover copy strings + button labels + spacing)
    - .planning/phases/13-inline-tune-highlights-grammarly-style-review-with-gap-promp/13-CONTEXT.md (D-17 popover variants, D-18 compact vs expanded rules, D-19 keyboard nav, D-20 auto-advance — but D-20 wiring is Plan 04, not here)
  </read_first>
  <behavior>
    Per Plan 01 ChangePopover.test.tsx (already in tree):
    - Renders nothing when activeChange is null (returns null directly, no portal).
    - Compact variant for severity='minor': renders an inline-style body with the alternative value as primary text + Accept + Skip buttons. Container has class containing 'compact' OR data-variant='compact'. Footer with keyboard hints (e.g., "← →") is present.
    - Expanded variant for severity='strong': renders BEFORE label + currentValue strikethrough block, AFTER label + alternatives[0].value block, plus the same Accept/Skip + nav row. Container has class containing 'expanded' OR data-variant='expanded'.
    - severity='add': renders expanded layout with NO BEFORE block (since currentValue is empty/missing), AFTER block reads "Add new {section} item".
    - severity='delete': renders expanded layout with BEFORE block strikethrough and AFTER block reading "Remove this item".
    - ArrowRight key fires onAdvance('next').
    - ArrowLeft key fires onAdvance('prev').
    - Enter key fires onAccept(activeChange.id).
    - Escape key fires onClose().
    - "Accept change" button click → onAccept.
    - "Skip change" button click → onSkip.
    - When activeChange changes (different id), the popover re-anchors to the new range without flicker (use anchorRange.getBoundingClientRect via floating-ui virtual element pattern).
    - When `anchorRange` is null but activeChange is non-null, popover renders centered (fallback) and a console.warn is emitted (RESEARCH §Pitfall 2). The fallback positioning MUST NOT crash.
  </behavior>
  <action>
    Build ChangePopover per RESEARCH.md Code Example 3 (lines 658-744) — that listing is the canonical reference.

    1. `frontend/src/features/direct-edit/components/ChangePopover.tsx` (NEW):
       - `import { useFloating, autoUpdate, flip, shift, offset, FloatingFocusManager, FloatingPortal, useDismiss, useInteractions, useRole } from '@floating-ui/react';` — verify each export exists in the installed version via the IDE; if `FloatingPortal` import fails, fall back to inline render and document in SUMMARY.
       - Compute `severity` -> variant: `const variant = severity === 'minor' ? 'compact' : 'expanded';` per D-18.
       - Use `useFloating({ open, onOpenChange, middleware: [offset(8), flip(), shift({ padding: 8 })], whileElementsMounted: autoUpdate })`.
       - Set `refs.setPositionReference({ getBoundingClientRect: () => anchorRange?.getBoundingClientRect() ?? DEFAULT_RECT })` where DEFAULT_RECT centers the popover at viewport mid (RESEARCH §Pitfall 2 fallback).
       - Use `useDismiss(context, { escapeKey: true, outsidePress: false })` — outsidePress is FALSE because the user must explicitly accept/skip (D-19 — the popover is modal-like in keyboard but non-modal in pointer; clicks outside the popover must NOT close it; only Escape does).
       - Wrap content in `<FloatingFocusManager context={context} modal={false} initialFocus={0}>` so Tab cycles inside the popover but doesn't trap globally.
       - Render ONE root `<div ref={refs.setFloating} style={floatingStyles} ...>`. Inside, branch on variant:
         * Compact body: `<div className={styles.compact}>` containing the AFTER text only + Accept + Skip + keyboard hint footer.
         * Expanded body: `<div className={styles.expanded}>` containing BEFORE block + arrow indicator + AFTER block + Accept + Skip + keyboard hint footer.
       - Keyboard handlers: attach `onKeyDown` to the floating div. ArrowRight/ArrowLeft → onAdvance; Enter → onAccept(activeChange.id); Escape → onClose.
       - Assertion: when `activeChange === null`, return `null` BEFORE calling useFloating to avoid hook order changes between renders. Use `if (!activeChange) return null;` at the very top.
         **CRITICAL** — this contradicts the conditional-hooks rule. To resolve, ALWAYS call useFloating, but return null AFTER hook calls. Standard React pattern: hooks always run, render is conditional. Adjust code accordingly.
       - The `open` prop fed into useFloating is `activeChange !== null`. onOpenChange handles Escape via `useDismiss`.

    2. `frontend/src/features/direct-edit/components/ChangePopover.module.css` (NEW):
       - `.popover` base: `background: var(--bg-primary); border: 1px solid var(--border-color); border-radius: var(--radius); box-shadow: var(--shadow-lg); padding: 12px; min-width: 280px; max-width: 480px; z-index: 1000;`.
       - `.compact { display: flex; flex-direction: column; gap: 8px; padding: 8px 12px; }`.
       - `.expanded { display: flex; flex-direction: column; gap: 12px; padding: 16px; }`.
       - `.beforeBlock { color: var(--text-muted); text-decoration: line-through; padding: 8px; background: var(--bg-tertiary); border-radius: var(--radius-sm); }`.
       - `.afterBlock { color: var(--text-primary); padding: 8px; background: var(--accent-light); border-radius: var(--radius-sm); border-left: 3px solid var(--accent); font-weight: 600; }` (use --success-light + --success border for severity='add').
       - `.actions { display: flex; gap: 8px; justify-content: flex-end; padding-top: 8px; border-top: 1px solid var(--border-color); }`.
       - `.acceptButton` mimics ChangeCard.module.css accept button styling (lines 100-130 in current ChangeCard.module.css — read first).
       - `.skipButton` mimics ChangeCard.module.css skip button styling.
       - `.keyboardHint { font-size: 11px; color: var(--text-muted); }`.
       - Severity accent classes: `.severityStrong { border-top: 3px solid var(--error); }` etc., applied to `.popover`.

    Reference:
    - PATTERNS.md lines 301-376 (ChangePopover pattern + variant rules)
    - RESEARCH.md Pattern 1 lines 230-279 (floating-ui middleware setup) + Example 3 (full skeleton)
    - UI-SPEC lines 125-180 (popover copy + spacing)
    - D-17/D-18/D-19 in CONTEXT.md
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/__tests__/ChangePopover.test.tsx --reporter=basic && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/__tests__/ChangePopover.test.tsx --reporter=basic` exits 0 (all assertions in the Plan 01 scaffold pass).
    - `cd frontend && npx tsc --noEmit` exits 0.
    - `grep -nE "useFloating\(" frontend/src/features/direct-edit/components/ChangePopover.tsx` returns ≥1 match.
    - `grep -n "FloatingFocusManager" frontend/src/features/direct-edit/components/ChangePopover.tsx` returns ≥1 match (focus mgmt wired).
    - `grep -nE "(ArrowRight|ArrowLeft|Enter|Escape)" frontend/src/features/direct-edit/components/ChangePopover.tsx` returns ≥4 matches (keyboard handlers present).
    - `grep -n "useDismiss" frontend/src/features/direct-edit/components/ChangePopover.tsx` returns ≥1 match (Escape close path).
    - `grep -v '^/\*\|^\s\*' frontend/src/features/direct-edit/components/ChangePopover.module.css | grep -c "compact\|expanded"` returns ≥2 (both variants styled).
    - `grep -nE "offset\(|flip\(|shift\(" frontend/src/features/direct-edit/components/ChangePopover.tsx` returns ≥3 matches (middleware composed correctly).
    - The component uses React JSX text rendering (not innerHTML) for AI-supplied alternatives — verify by `! grep -n "innerHTML\|dangerouslySetInnerHTML" frontend/src/features/direct-edit/components/ChangePopover.tsx` (T-13-03-01 mitigation).
  </acceptance_criteria>
  <done>
    ChangePopover renders compact + expanded variants, anchors via floating-ui virtual element, supports the four keyboard shortcuts, and turns the Plan 01 scaffold green. tsc clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GapPromptChips click-to-expand input</name>
  <files>frontend/src/features/direct-edit/components/GapPromptChips.tsx, frontend/src/features/direct-edit/components/GapPromptChips.module.css</files>
  <read_first>
    - frontend/src/features/direct-edit/components/TunePanel.tsx (lines 421-427 — existing inline-textarea reveal pattern)
    - frontend/src/features/direct-edit/components/ChangeCard.tsx (lines 104-119 — existing chip styling analog)
    - frontend/src/__tests__/GapPromptChips.test.tsx (Plan 01 scaffold — exact assertions this component must satisfy)
    - .planning/phases/13-inline-tune-highlights-grammarly-style-review-with-gap-promp/13-PATTERNS.md (lines 218-255 GapPromptChips pattern; click-to-expand + onClarificationsChange filtering rule)
    - .planning/phases/13-inline-tune-highlights-grammarly-style-review-with-gap-promp/13-UI-SPEC.md (lines 64-85 chip copy + visual spec; line 86 "Click to add details" placeholder)
    - .planning/phases/13-inline-tune-highlights-grammarly-style-review-with-gap-promp/13-CONTEXT.md (D-04, D-05, D-06 — exact behavior contract)
    - frontend/src/styles/variables.css (--bg-tertiary, --bg-hover, --accent, --accent-light, --radius, --radius-sm)
  </read_first>
  <behavior>
    Per the Plan 01 scaffold tests:
    - missing=['Spanish', 'Kubernetes', 'Team leadership'] renders three buttons (one per gap) with the gap text visible.
    - missing=[] (empty) renders the heading "No gaps detected" (UI-SPEC line 76 copy).
    - Click chip[0] → reveals an `<textarea>` (or input — either is acceptable; pick textarea per UI-SPEC line 80) below it with placeholder "Click to add details" and aria-label including the gap text.
    - Typing into the input does NOT call onClarificationsChange (debounce until blur).
    - Blur of the input calls onClarificationsChange with `['<typed text>']` — array contains ONLY the touched chip's text, not all chips.
    - Clicking a second chip while the first is expanded collapses the first AND opens the second (single-expanded-at-a-time per UX rule).
    - Clearing the textarea contents and blurring REMOVES that chip's clarification from the next onClarificationsChange emit.
    - Length cap: textarea has `maxLength={500}` per RESEARCH §Pitfall 8 (input validation defense in depth even though backend caps too).
    - Count cap: if 12+ chips render but only 8 are touched, onClarificationsChange emits an array of 8 (one per touched chip, in chip order). 12+ is the chip array's natural cap from match-analysis.

    Chip semantics:
    - chip[i] state = { expanded: boolean, value: string }
    - The component owns local state; emits onClarificationsChange ONLY on blur of an active textarea, with the full filtered array.

    NEW tests authored in this task (extend frontend/src/__tests__/GapPromptChips.test.tsx if it has only the basic Plan 01 scaffold assertions; otherwise add a new file `GapPromptChips.behavior.test.tsx`):
    - Single-chip-expanded-at-a-time invariant.
    - Length cap enforcement.
    - Clearing a touched chip removes its clarification.
  </behavior>
  <action>
    Per PATTERNS.md lines 218-255.

    1. `frontend/src/features/direct-edit/components/GapPromptChips.tsx` (NEW):
       - Functional component, named export `GapPromptChips`.
       - Local state: `const [expandedIndex, setExpandedIndex] = useState<number | null>(null);` and `const [values, setValues] = useState<string[]>(() => missing.map(() => ''));`.
       - Reset values when `missing` array reference changes: `useEffect(() => { setValues(missing.map(() => '')); setExpandedIndex(null); }, [missing])`. (Clears local state if a new match-analysis arrives.)
       - Render: `<div className={styles.chipArray}>{missing.length === 0 ? <p className={styles.empty}>No gaps detected</p> : missing.map((gap, i) => (...))}</div>`.
       - Each chip renders as a `<button type="button">` with text = gap. Click → `setExpandedIndex(prev => prev === i ? null : i)`.
       - When `expandedIndex === i`, render a `<textarea>` immediately AFTER the button (sibling, in a `<div className={styles.chipGroup}>`). Textarea props: `value={values[i]}`, `onChange={e => setValues(v => v.map((x, idx) => idx === i ? e.target.value : x))}`, `onBlur={() => emitClarifications()}`, `placeholder="Click to add details"`, `maxLength={500}`, `aria-label={`Add details for ${gap}`}`.
       - `emitClarifications`: `const trimmed = values.map(v => v.trim()).filter(Boolean); onClarificationsChange(trimmed);`.
       - Empty-state heading: `<h4 className={styles.emptyHeading}>No gaps detected</h4>` per UI-SPEC line 76.
       - Auto-focus the textarea when it opens (`autoFocus` prop on textarea OR ref-based focus in useEffect on `expandedIndex` change).

    2. `frontend/src/features/direct-edit/components/GapPromptChips.module.css` (NEW):
       - `.chipArray { display: flex; flex-wrap: wrap; gap: 8px; padding: 12px; }`.
       - `.chip { padding: 6px 12px; background: var(--bg-tertiary); border: 1px solid var(--border-color); border-radius: var(--radius); color: var(--text-primary); cursor: pointer; transition: all 0.18s ease; font-size: 13px; }`.
       - `.chip:hover { background: var(--bg-hover); }`.
       - `.chipExpanded { background: var(--accent-light); border-color: var(--accent); color: var(--accent); }`.
       - `.chipGroup { display: flex; flex-direction: column; gap: 6px; flex-basis: 100%; }` (forces wrap so textarea always sits below its chip).
       - `.textarea { padding: 8px 12px; border: 1px solid var(--accent); border-radius: var(--radius-sm); resize: vertical; min-height: 60px; max-height: 160px; font-family: inherit; font-size: 13px; color: var(--text-primary); background: var(--bg-primary); }`.
       - `.textarea:focus { outline: none; box-shadow: 0 0 0 3px var(--accent-light); }`.
       - `.emptyHeading { color: var(--text-muted); padding: 12px; text-align: center; font-weight: 400; font-size: 14px; }`.

    3. Extend `frontend/src/__tests__/GapPromptChips.test.tsx` (Plan 01 scaffold) with three new assertions:
       - `it('keeps only one chip expanded at a time')` — open chip[0], open chip[1], assert chip[0]'s textarea no longer in document.
       - `it('enforces 500-char maxLength on textarea')` — assert textarea's `maxLength` attribute is 500.
       - `it('removes a cleared chip from emitted clarifications')` — type, blur (assert called with [text]), clear, blur (assert called with []).

    Reference:
    - PATTERNS.md lines 218-255 (chip pattern + emit rule)
    - UI-SPEC lines 64-85 (chip copy + visual)
    - D-04, D-05, D-06 in CONTEXT.md (chip-array as match-analysis seed; click-to-expand; clarifications array exclusive to touched chips)
    - RESEARCH §Pitfall 8 (input validation cap)
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/__tests__/GapPromptChips.test.tsx --reporter=basic && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/__tests__/GapPromptChips.test.tsx --reporter=basic` exits 0 (Plan 01 scaffold + three new tests authored here all pass).
    - `cd frontend && npx tsc --noEmit` exits 0.
    - `grep -n "onClarificationsChange" frontend/src/features/direct-edit/components/GapPromptChips.tsx` returns ≥1 match.
    - `grep -n "maxLength={500}" frontend/src/features/direct-edit/components/GapPromptChips.tsx` returns ≥1 match (length cap enforced).
    - `grep -nE "trim\(\)|\.filter\(Boolean\)" frontend/src/features/direct-edit/components/GapPromptChips.tsx` returns ≥1 match (empty/whitespace filter on emit).
    - `grep -n "No gaps detected" frontend/src/features/direct-edit/components/GapPromptChips.tsx` returns ≥1 match (empty-state copy per UI-SPEC).
    - `grep -v '^/\*\|^\s\*' frontend/src/features/direct-edit/components/GapPromptChips.module.css | grep -c "\.chip\|\.textarea\|\.chipGroup"` returns ≥3.
  </acceptance_criteria>
  <done>
    GapPromptChips renders chip-array, click reveals inline textarea, blur emits filtered clarifications array (only touched chips). Plan 01 scaffold turns green; three additional behavior tests added; tsc clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI-supplied alternatives[0].value → ChangePopover JSX text | Existing — same as Plan 02 ghost bullet boundary; React JSX text escapes by default. |
| User-typed clarifications text → GapPromptChips local state → emitted array → backend | New: free-text user input crosses chip boundary into the AI prompt (T-13-03-02). |
| @floating-ui/react computed positions from anchorRange.getBoundingClientRect | Existing DOM API, no untrusted input — anchor Range comes from useChangeHighlights which is internally derived. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03-01 | Tampering / XSS | ChangePopover renders alternatives[0].value as JSX text | accept (mitigated by JSX) | React JSX `{value}` already HTML-escapes. The popover MUST NOT use innerHTML or dangerouslySetInnerHTML. Acceptance criteria includes `grep -v innerHTML` check. ASVS V5.3.3. |
| T-13-03-02 | Tampering | GapPromptChips emits user text into clarifications array sent to backend | mitigate | Frontend enforces `maxLength={500}` per textarea (defense in depth alongside backend filtering in Plan 01). Trim/filter on emit removes whitespace-only entries. Backend has its own validation (Plan 01 Test 5 — non-string elements rejected by Pydantic). ASVS V5.1.4 + V13.1.3. |
| T-13-03-03 | DoS | Anchor recompute storm via floating-ui autoUpdate | accept | autoUpdate is debounced internally by floating-ui via rAF. Not a phase-level concern. ASVS V11.1.4. |
| T-13-03-04 | Information Disclosure | Focus trap inside FloatingFocusManager modal=false | accept | modal={false} means screen readers can still reach the rest of the page; that's intentional (D-19 — the popover is keyboard-modal but assistive-tech non-modal). Verified in PATTERNS.md §Floating focus rules. |
| T-13-03-05 | Tampering | Supply-chain risk from new @floating-ui/react dependency | mitigate | `@floating-ui/react@^0.27.19` is a widely-used library (1.5M weekly downloads per RESEARCH.md). Pin to caret-minor (^0.27.x) and verify post-install resolves to a single instance via `npm ls @floating-ui/react`. ASVS V14.2.1. |

**High-severity threats blocking the plan:** None. T-13-03-02 mitigation is mandatory and is encoded in the maxLength + trim/filter acceptance criteria.
</threat_model>

<verification>
- `cd frontend && npx vitest run src/__tests__/ChangeHighlight.test.tsx src/__tests__/ChangePopover.test.tsx src/__tests__/GapPromptChips.test.tsx --reporter=basic` → all green.
- `cd frontend && npx tsc --noEmit` → exits 0.
- `cd frontend && npx eslint src/features/direct-edit/components/ChangePopover.tsx src/features/direct-edit/components/ChangeHighlight.tsx src/features/direct-edit/components/GapPromptChips.tsx --max-warnings 0` → exits 0.
- `cd frontend && npm ls @floating-ui/react` → single resolved version, no UNMET PEER DEPENDENCY.
- `! grep -n "innerHTML\|dangerouslySetInnerHTML" frontend/src/features/direct-edit/components/ChangePopover.tsx frontend/src/features/direct-edit/components/ChangeHighlight.tsx frontend/src/features/direct-edit/components/GapPromptChips.tsx` (T-13-03-01 mitigation).
</verification>

<success_criteria>
- @floating-ui/react@^0.27.19 installed and resolvable
- ChangeHighlight renders span with severity-tier squiggle classes
- ChangePopover renders compact (minor) + expanded (strong/add/delete) variants per D-18
- ChangePopover supports ArrowRight/Left/Enter/Escape keyboard nav per D-19
- ChangePopover anchors via floating-ui virtual element to anchorRange
- GapPromptChips renders one chip per missing[] entry per D-04
- GapPromptChips reveals inline textarea on chip click per D-05
- GapPromptChips emits onClarificationsChange with only touched chips per D-06
- GapPromptChips enforces 500-char maxLength
- All Wave 0 ChangePopover + GapPromptChips scaffolds turn green
- New ChangeHighlight tests (5) pass
- tsc + eslint clean
</success_criteria>

<output>
After completion, create `.planning/phases/13-inline-tune-highlights-grammarly-style-review-with-gap-promp/13-03-SUMMARY.md` summarizing:
- New dependency: @floating-ui/react@<resolved-version>
- Three new components + CSS modules + tests
- Plan 01 scaffold green-status (ChangePopover + GapPromptChips)
- ChangeHighlight test count (5 passing)
- Open Plan 04 contract handoffs (ChangePopoverProps, ChangeHighlightProps, GapPromptChipsProps; rail composes all three)
- Notes on any floating-ui version drift (e.g., resolved 0.27.20 vs 0.27.19)
</output>
