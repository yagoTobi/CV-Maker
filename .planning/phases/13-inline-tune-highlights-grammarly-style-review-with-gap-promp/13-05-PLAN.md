---
phase: 13
plan: 05
type: execute
wave: 4
depends_on: [04]
files_modified:
  - frontend/src/features/direct-edit/components/TunePanel.tsx
  - frontend/src/features/direct-edit/components/TunePanel.module.css
  - frontend/src/features/direct-edit/components/ChangePanel.tsx
  - frontend/src/features/direct-edit/components/ChangePanel.module.css
  - frontend/src/features/direct-edit/components/ChangeCard.tsx
  - frontend/src/features/direct-edit/components/ChangeCard.module.css
  - frontend/src/features/direct-edit/hooks/useScrollSync.ts
autonomous: true
requirements:
  - D-01
user_setup: []

must_haves:
  truths:
    - "TunePanel.tsx + .module.css are deleted (Phase 13 D-01 — TuneRail replaces it)"
    - "ChangePanel.tsx + .module.css are deleted (consumed only by TunePanel)"
    - "ChangeCard.tsx + .module.css are deleted (consumed only by ChangePanel)"
    - "useScrollSync.ts is deleted (consumed only by TunePanel)"
    - "No remaining imports reference the deleted files anywhere in src/"
    - "TypeScript compiles clean after deletion"
    - "Full Vitest suite remains green after deletion"
    - "ESLint passes with no warnings"
  artifacts:
    - path: "frontend/src/features/direct-edit/components/TunePanel.tsx"
      provides: "DELETED — replaced by TuneRail in Plan 04"
    - path: "frontend/src/features/direct-edit/components/ChangePanel.tsx"
      provides: "DELETED — folded into TuneRail's review-shrunk state"
    - path: "frontend/src/features/direct-edit/components/ChangeCard.tsx"
      provides: "DELETED — replaced by ChangePopover (Plan 03) and inline highlights"
    - path: "frontend/src/features/direct-edit/hooks/useScrollSync.ts"
      provides: "DELETED — consumed only by TunePanel; TuneRail uses useChangeHighlights for scroll anchoring"
  key_links:
    - from: "frontend/src/features/direct-edit"
      to: "(none)"
      via: "no remaining imports of deleted files"
      pattern: "TunePanel|ChangePanel|ChangeCard|useScrollSync"
---

<objective>
Delete the legacy split-screen tune flow files that Plan 04 made dead code: `TunePanel.tsx` + `.module.css`, `ChangePanel.tsx` + `.module.css`, `ChangeCard.tsx` + `.module.css`, and `useScrollSync.ts`. Verify no callers remain, tsc + eslint + the full Vitest suite stay green, and the Phase 13 success-criteria item "no Grammarly-feature regressions" holds.

Purpose: Until this plan, the legacy tree has been preserved so Plan 04 had analog code to port from and so any straggler imports could be flushed in one pass. With TuneRail in place and DirectEditPage rewired, these four files (plus their CSS modules) are pure dead weight.

Output: Seven deleted files, zero stale imports, all gates green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-inline-tune-highlights-grammarly-style-review-with-gap-promp/13-CONTEXT.md
@.planning/phases/13-inline-tune-highlights-grammarly-style-review-with-gap-promp/13-PATTERNS.md
@.planning/phases/13-inline-tune-highlights-grammarly-style-review-with-gap-promp/13-04-SUMMARY.md

<interfaces>
<!-- No new interfaces. This is a deletion plan. -->
<!-- All consumers of the deleted files were rewritten in Plan 04. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit consumers + delete legacy files</name>
  <files>frontend/src/features/direct-edit/components/TunePanel.tsx, frontend/src/features/direct-edit/components/TunePanel.module.css, frontend/src/features/direct-edit/components/ChangePanel.tsx, frontend/src/features/direct-edit/components/ChangePanel.module.css, frontend/src/features/direct-edit/components/ChangeCard.tsx, frontend/src/features/direct-edit/components/ChangeCard.module.css, frontend/src/features/direct-edit/hooks/useScrollSync.ts</files>
  <read_first>
    - frontend/src/features/direct-edit/components/TunePanel.tsx (FULL — last read before deletion to confirm Plan 04 ported every behavior)
    - frontend/src/features/direct-edit/components/ChangePanel.tsx (FULL — confirm only TunePanel imports it)
    - frontend/src/features/direct-edit/components/ChangeCard.tsx (FULL — confirm only ChangePanel imports it)
    - frontend/src/features/direct-edit/hooks/useScrollSync.ts (FULL — confirm only TunePanel imports it)
    - .planning/phases/13-inline-tune-highlights-grammarly-style-review-with-gap-promp/13-PATTERNS.md (analog mapping table — verifies every behavior was ported)
    - frontend/src/utils/wordDiff.ts (if exists — was it used only by ChangeCard? If so, it should also be deleted; if used elsewhere, leave it)
  </read_first>
  <behavior>
    Pre-deletion audit (executor MUST run BEFORE deleting):
    1. Run `grep -rn "TunePanel\|ChangePanel\|ChangeCard\|useScrollSync" frontend/src/ --include="*.ts" --include="*.tsx"` and verify the ONLY matches are:
       - The files about to be deleted themselves
       - Possibly an old test file under `frontend/src/__tests__/` (e.g., TunePanel.test.tsx) — if it exists, it must also be deleted in this task
       - NO matches in DirectEditPage.tsx, MedLengthTemplate.tsx, NavBar.tsx, or any feature/route component
    2. Check if `frontend/src/utils/wordDiff.ts` exists. If yes:
       - `grep -rn "wordDiff" frontend/src/ --include="*.ts" --include="*.tsx"` — if its only consumer was ChangeCard, add it to the deletion list. If used by useChangeHighlights or ChangePopover or any Plan 02/03/04 file, leave it.
    3. After deletion, the same grep must return zero matches.

    Post-deletion verification:
    - tsc clean.
    - Full Vitest suite green.
    - ESLint clean (no max-warnings-0 violations).
    - `npm run build` (production build) succeeds — sometimes Vite's tree-shaker catches stale references that tsc misses.
  </behavior>
  <action>
    1. **Audit phase** (run grep commands listed in <behavior>):
       ```
       grep -rn "TunePanel\|ChangePanel\|ChangeCard\|useScrollSync" frontend/src/ --include="*.ts" --include="*.tsx"
       ```
       Capture the output. The only file paths in the result MUST be the files-to-delete themselves OR test files for those specific components. If ANY other file matches (e.g., a stray import in a feature component or a test that wasn't updated), HALT and surface the find before deleting — that means Plan 04 missed a consumer and we need to fix Plan 04 first.

    2. **wordDiff.ts check** (only if file exists):
       ```
       ls frontend/src/utils/wordDiff.ts 2>/dev/null && grep -rn "wordDiff" frontend/src/ --include="*.ts" --include="*.tsx" | grep -v "^frontend/src/utils/wordDiff.ts" | grep -v "^frontend/src/features/direct-edit/components/ChangeCard\|^frontend/src/__tests__/.*[Cc]hangeCard"
       ```
       If the second grep produces ANY output, `wordDiff.ts` has live consumers — leave it. If empty, add `frontend/src/utils/wordDiff.ts` to the deletion list and re-run the same grep after deletion.

    3. **Delete the files** (use `rm` via bash):
       ```
       rm frontend/src/features/direct-edit/components/TunePanel.tsx
       rm frontend/src/features/direct-edit/components/TunePanel.module.css
       rm frontend/src/features/direct-edit/components/ChangePanel.tsx
       rm frontend/src/features/direct-edit/components/ChangePanel.module.css
       rm frontend/src/features/direct-edit/components/ChangeCard.tsx
       rm frontend/src/features/direct-edit/components/ChangeCard.module.css
       rm frontend/src/features/direct-edit/hooks/useScrollSync.ts
       # If TunePanel.test.tsx etc. exist:
       rm -f frontend/src/__tests__/TunePanel.test.tsx
       rm -f frontend/src/__tests__/ChangePanel.test.tsx
       rm -f frontend/src/__tests__/ChangeCard.test.tsx
       rm -f frontend/src/__tests__/useScrollSync.test.ts
       ```

    4. **Post-deletion grep** (must return zero matches):
       ```
       grep -rn "TunePanel\|ChangePanel\|ChangeCard\|useScrollSync" frontend/src/ --include="*.ts" --include="*.tsx"
       ```

    5. **Type-check + lint + test + build**:
       ```
       cd frontend && npx tsc --noEmit
       cd frontend && npx eslint . --max-warnings 0
       cd frontend && npx vitest run --reporter=basic
       cd frontend && npm run build
       ```
       All four must exit 0. If tsc surfaces a leftover import, the audit missed something — fix that file's import (replace with TuneRail/ChangePopover/etc. as appropriate per Plan 04) and re-run.

    6. **Backend regression check**:
       ```
       cd backend && pytest -x -q -m "not slow"
       ```
       Must exit 0. (Phase 13 backend changes from Plan 01 should be unaffected by frontend deletions; this is a defensive sanity check.)

    Reference:
    - PATTERNS.md analog mapping table (Plan 04 ported behavior verbatim from these files; deletion is safe by construction)
    - D-01 in CONTEXT.md (TunePanel removal is the explicit endpoint of the phase)
  </action>
  <verify>
    <automated>! grep -rn "TunePanel\|ChangePanel\|ChangeCard\|useScrollSync" frontend/src/ --include="*.ts" --include="*.tsx" && cd frontend && npx tsc --noEmit && npx vitest run --reporter=basic && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `! ls frontend/src/features/direct-edit/components/TunePanel.tsx 2>/dev/null` (file does not exist).
    - `! ls frontend/src/features/direct-edit/components/TunePanel.module.css 2>/dev/null`.
    - `! ls frontend/src/features/direct-edit/components/ChangePanel.tsx 2>/dev/null`.
    - `! ls frontend/src/features/direct-edit/components/ChangePanel.module.css 2>/dev/null`.
    - `! ls frontend/src/features/direct-edit/components/ChangeCard.tsx 2>/dev/null`.
    - `! ls frontend/src/features/direct-edit/components/ChangeCard.module.css 2>/dev/null`.
    - `! ls frontend/src/features/direct-edit/hooks/useScrollSync.ts 2>/dev/null`.
    - `grep -rn "TunePanel\|ChangePanel\|ChangeCard\|useScrollSync" frontend/src/ --include="*.ts" --include="*.tsx" | wc -l` returns 0.
    - `cd frontend && npx tsc --noEmit` exits 0.
    - `cd frontend && npx eslint . --max-warnings 0` exits 0.
    - `cd frontend && npx vitest run --reporter=basic` exits 0 (full suite green).
    - `cd frontend && npm run build` exits 0 (production build succeeds, tree-shaker happy).
    - `cd backend && pytest -x -q -m "not slow"` exits 0 (no backend regression).
  </acceptance_criteria>
  <done>
    Seven legacy files deleted, zero stale imports remain, full TypeScript/lint/test/build pipeline green for both frontend and backend. Phase 13's D-01 endpoint reached: TunePanel is no more.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan introduces no new trust boundaries. It deletes existing code paths that themselves had no remaining consumers after Plan 04.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05-01 | Tampering | Stale import accidentally bundled into production via Vite's `import.meta.glob` or similar dynamic resolution | mitigate | The `npm run build` acceptance criterion catches this — if Vite encounters a missing module reference, the production build fails. Both static (`grep`) and dynamic (`npm run build`) checks are in place. ASVS V14.2.1 (dependency integrity). |
| T-13-05-02 | Information Disclosure | Deleted file accidentally retained in git history exposes legacy AI prompt logic | accept | These files are already public in git history pre-deletion. No new exposure. The new TuneRail uses the same prompt path through Plan 01's backend. ASVS V8.1 — no PII or secrets in deleted code. |

**High-severity threats blocking the plan:** None. Cleanup-only plan.
</threat_model>

<verification>
- `! grep -rn "TunePanel\|ChangePanel\|ChangeCard\|useScrollSync" frontend/src/ --include="*.ts" --include="*.tsx"` → no matches.
- `cd frontend && npx tsc --noEmit` → exits 0.
- `cd frontend && npx eslint . --max-warnings 0` → exits 0.
- `cd frontend && npx vitest run --reporter=basic` → all tests green.
- `cd frontend && npm run build` → exits 0 (production bundle builds without missing-module errors).
- `cd backend && pytest -x -q -m "not slow"` → exits 0.
</verification>

<success_criteria>
- All seven legacy files deleted (TunePanel + ChangePanel + ChangeCard + useScrollSync, each with its CSS module where applicable)
- Zero remaining grep references to the deleted module names anywhere in src/
- tsc clean
- ESLint clean
- Full Vitest suite green
- Production build succeeds
- Backend pytest green (no Phase 13 regression)
</success_criteria>

<output>
After completion, create `.planning/phases/13-inline-tune-highlights-grammarly-style-review-with-gap-promp/13-05-SUMMARY.md` summarizing:
- Files deleted (with line counts removed for posterity)
- Whether wordDiff.ts was deleted (yes/no, with rationale)
- Whether any test files were also deleted (TunePanel.test.tsx, etc.)
- Whether any imports needed last-mile fixing (should be 0 if Plan 04 was clean)
- Final tsc + lint + test + build status
- Phase 13 cleanup endpoint confirmation: TunePanel is gone, TuneRail is the canonical tune surface
</output>
