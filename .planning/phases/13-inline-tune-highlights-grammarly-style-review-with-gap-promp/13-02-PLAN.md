---
phase: 13
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - frontend/src/features/direct-edit/utils/severity.ts
  - frontend/src/features/direct-edit/hooks/useChangeHighlights.ts
  - frontend/src/features/direct-edit/components/EditableField.tsx
  - frontend/src/features/direct-edit/components/EditableField.module.css
  - frontend/src/features/direct-edit/components/EditableBulletList.tsx
  - frontend/src/features/direct-edit/components/EditableBulletList.module.css
  - frontend/src/hooks/useTailor.ts
  - frontend/src/services/api.ts
autonomous: true
requirements:
  - D-07
  - D-12
  - D-13
  - D-14
  - D-15
  - D-16
user_setup: []

must_haves:
  truths:
    - "Severity is inferred client-side from changeType + word-overlap diff per D-12 rules"
    - "useChangeHighlights returns activeChangeId, pendingChanges, severityMap, getRangeForChange, advanceTo, dismissSection"
    - "EditableField accepts optional highlightSpans prop and injects squiggle <span> wrappers ONLY when not focused"
    - "First keystroke inside a highlighted region triggers tailor.skipChange(activeChangeId) per D-16 (auto-dismiss)"
    - "EditableBulletList renders ghost bullets (contentEditable=false) for add-type changes per D-15 and strikethrough for delete-type"
    - "useTailor.fetchSuggestions accepts optional userClarifications: string[] and forwards to api.suggestTailorChanges"
    - "api.suggestTailorChanges sends user_clarifications snake_case in the POST body when provided"
  artifacts:
    - path: "frontend/src/features/direct-edit/utils/severity.ts"
      provides: "inferSeverity function + Severity type per D-12"
      exports: ["inferSeverity", "Severity"]
    - path: "frontend/src/features/direct-edit/hooks/useChangeHighlights.ts"
      provides: "Highlight state + DOM range mapping hook"
      exports: ["useChangeHighlights"]
      contains: "documentOrderIds"
    - path: "frontend/src/features/direct-edit/components/EditableField.tsx"
      provides: "highlightSpans prop with non-focus-disturbing injection + onAutoDismiss"
    - path: "frontend/src/features/direct-edit/components/EditableBulletList.tsx"
      provides: "Ghost bullet rendering for add-type + delete strikethrough plumbing"
    - path: "frontend/src/hooks/useTailor.ts"
      provides: "fetchSuggestions accepts userClarifications param"
    - path: "frontend/src/services/api.ts"
      provides: "suggestTailorChanges accepts userClarifications param"
  key_links:
    - from: "frontend/src/features/direct-edit/hooks/useChangeHighlights.ts"
      to: "frontend/src/features/direct-edit/utils/severity.ts"
      via: "import { inferSeverity } and call per change in useMemo"
      pattern: "inferSeverity\\("
    - from: "frontend/src/features/direct-edit/components/EditableField.tsx"
      to: "useChangeHighlights"
      via: "highlightSpans prop fed by parent (Plan 04 will wire this)"
      pattern: "highlightSpans"
    - from: "frontend/src/services/api.ts"
      to: "backend/routes/tailor.py"
      via: "POST /tailor/suggest-changes body field user_clarifications"
      pattern: "user_clarifications:"
    - from: "frontend/src/hooks/useTailor.ts"
      to: "frontend/src/services/api.ts"
      via: "fetchSuggestions forwards userClarifications to api.suggestTailorChanges"
      pattern: "suggestTailorChanges\\("
---

<objective>
Build the inline-highlight foundation: a deterministic client-side severity util (D-12), the `useChangeHighlights` hook that maps `TailorChange[]` to live DOM ranges (D-13/D-14/D-19/D-20 enabler), and `EditableField`/`EditableBulletList` extensions that accept `highlightSpans` and ghost-bullet placeholders without breaking the existing uncontrolled-when-focused contract (D-15/D-16). Also extend `useTailor` and `api.ts` to forward `userClarifications` to the backend wired by Plan 01 (D-07).

Purpose: Plans 03 and 04 both consume this foundation. The popover (Plan 03) needs `getRangeForChange` and `severityMap`. The rail (Plan 04) needs `userClarifications` plumbing through to `useTailor`. Splitting it out keeps Plans 03 and 04 simpler and lets Plan 03 run in parallel with this one is NOT possible — Plan 03 imports `inferSeverity` and the hook, so Plan 03 depends on Plan 02. (See dependency note in plan frontmatter — actually Plan 03 will mock these in unit tests, but real integration in Plan 04 needs both.)

Output: One pure util, one hook, two component extensions, two service-layer signature updates, all aimed at turning the Plan 01 Wave 0 scaffolds for `severity.test.ts` and `useChangeHighlights.test.ts` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-inline-tune-highlights-grammarly-style-review-with-gap-promp/13-CONTEXT.md
@.planning/phases/13-inline-tune-highlights-grammarly-style-review-with-gap-promp/13-RESEARCH.md
@.planning/phases/13-inline-tune-highlights-grammarly-style-review-with-gap-promp/13-PATTERNS.md
@.planning/phases/13-inline-tune-highlights-grammarly-style-review-with-gap-promp/13-UI-SPEC.md
@.planning/phases/13-inline-tune-highlights-grammarly-style-review-with-gap-promp/13-01-SUMMARY.md
@frontend/src/features/direct-edit/components/EditableField.tsx
@frontend/src/features/direct-edit/components/EditableBulletList.tsx
@frontend/src/hooks/useTailor.ts
@frontend/src/services/api.ts
@frontend/src/types/index.ts
@frontend/src/utils/formDataPatch.ts

<interfaces>
<!-- Types and exports the executor will produce or consume -->

NEW — frontend/src/features/direct-edit/utils/severity.ts:
```typescript
export type Severity = 'strong' | 'minor' | 'add' | 'delete';
export function inferSeverity(change: TailorChange): Severity;
```

NEW — frontend/src/features/direct-edit/hooks/useChangeHighlights.ts (signature):
```typescript
import type { RefObject } from 'react';
import type { TailorChange } from '../../../types';
import type { Severity } from '../utils/severity';

export interface UseChangeHighlightsResult {
  activeChangeId: string | null;
  setActiveChangeId: (id: string | null) => void;
  pendingChanges: TailorChange[];
  severityMap: Map<string, Severity>;
  getRangeForChange: (id: string) => Range | null;
  documentOrderIds: string[];
  advanceTo: (direction: 'next' | 'prev') => string | null;
  dismissSection: (sectionKey: string) => string[]; // returns the change IDs that were skipped
}

export function useChangeHighlights(
  changes: TailorChange[],
  appliedChanges: Set<string>,
  skippedChanges: Set<string>,
  cvContainerRef: RefObject<HTMLElement | null>,
): UseChangeHighlightsResult;
```

EXISTING (already in repo) — TailorChange (frontend/src/types/index.ts ~lines 145-164):
```typescript
export interface TailorChange {
  id: string;
  fieldPath: string;            // e.g., "workExperience[0].bullets[2]"
  changeType: 'modify' | 'add' | 'remove';
  currentValue: unknown;
  alternatives: TailorAlternative[];
  description: string;
  section: string;
}
```

EXTEND — frontend/src/hooks/useTailor.ts (current line 17):
```typescript
// Before:
fetchSuggestions: (formData: CVFormData, jobDesc: string, company: string, role: string) => Promise<void>;
// After (this plan):
fetchSuggestions: (formData: CVFormData, jobDesc: string, company: string, role: string, userClarifications?: string[]) => Promise<void>;
```

EXTEND — frontend/src/services/api.ts (current line ~192-215):
```typescript
async suggestTailorChanges(
  formData: CVFormData,
  jobDescription: string,
  companyName?: string,
  role?: string,
  userClarifications?: string[],   // NEW
  signal?: AbortSignal,
): Promise<TailorResponse | null>
```

EXTEND — frontend/src/features/direct-edit/components/EditableField.tsx (props):
```typescript
export interface HighlightSpan {
  changeId: string;
  severity: Severity;
  isActive: boolean;
  startOffset: number;
  endOffset: number;
}
interface EditableFieldProps {
  // ...existing props
  highlightSpans?: HighlightSpan[];
  onAutoDismiss?: (changeId: string) => void; // first-keystroke handler (D-16)
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: severity.ts util + useChangeHighlights hook</name>
  <files>frontend/src/features/direct-edit/utils/severity.ts, frontend/src/features/direct-edit/hooks/useChangeHighlights.ts</files>
  <read_first>
    - frontend/src/__tests__/severity.test.ts (Plan 01 Task 2 scaffold — exact assertions this util must satisfy)
    - frontend/src/__tests__/useChangeHighlights.test.ts (Plan 01 Task 2 scaffold — exact behaviors this hook must satisfy)
    - frontend/src/hooks/useTailor.ts (analog: Set state, useCallback returns, useMemo derived values — lines 38-72, 220-239)
    - frontend/src/utils/formDataPatch.ts (existing fieldPathToSection — used by dismissSection)
    - frontend/src/types/index.ts (TailorChange / TailorAlternative shapes)
    - .planning/phases/13-inline-tune-highlights-grammarly-style-review-with-gap-promp/13-RESEARCH.md (lines 313-345 severity algorithm; lines 492-619 useChangeHighlights skeleton)
    - .planning/phases/13-inline-tune-highlights-grammarly-style-review-with-gap-promp/13-PATTERNS.md (lines 384-415 hook pattern + section-dismiss)
    - .planning/phases/13-inline-tune-highlights-grammarly-style-review-with-gap-promp/13-CONTEXT.md (D-12 algorithm, D-23 section dismiss)
  </read_first>
  <behavior>
    severity.ts:
    - inferSeverity({changeType:'add'}) === 'add'
    - inferSeverity({changeType:'remove'}) === 'delete'
    - inferSeverity({changeType:'modify', currentValue:'Led team of 5', alternatives:[{value:'Led team of 12'}]}) === 'minor' (high overlap)
    - inferSeverity({changeType:'modify', currentValue:'Led a small team', alternatives:[{value:'Drove rapid growth across product surface'}]}) === 'strong' (low overlap)
    - inferSeverity for empty currentValue treats overlap as 0 → returns 'strong' (per RESEARCH algorithm: `if (!a) return 1` then `1 - 0 = 1` ratio).

    useChangeHighlights:
    - pendingChanges = changes.filter(c => !applied.has(c.id) && !skipped.has(c.id))
    - severityMap.size === changes.length (one entry per change, not just pending)
    - documentOrderIds is the pendingChanges sorted by the index of c.fieldPath in the cvContainer's [data-field-path] node order
    - getRangeForChange returns null when changeId not found OR cvContainerRef.current is null
    - getRangeForChange for changeType='modify' with matching substring returns Range whose start is inside the matched text node and whose toString equals the substring
    - getRangeForChange for changeType='add' returns a collapsed Range at end of fieldEl
    - getRangeForChange for changeType='remove' returns a Range over the entire fieldEl content
    - advanceTo('next') with activeChangeId=null returns documentOrderIds[0]
    - advanceTo('next') from middle returns documentOrderIds[i+1]
    - advanceTo('prev') from index 0 returns null
    - dismissSection(sectionKey) returns the IDs of pendingChanges whose fieldPathToSection(fieldPath) === sectionKey (Plan 04 wires the actual skipChange calls).
  </behavior>
  <action>
    Implement severity.ts and useChangeHighlights.ts per RESEARCH.md and PATTERNS.md.

    1. `frontend/src/features/direct-edit/utils/severity.ts` (NEW):
       - Export `Severity` union type: `'strong' | 'minor' | 'add' | 'delete'`.
       - Implement `inferSeverity` per RESEARCH §Pattern 3 lines 313-345 verbatim:
         * `add` → 'add', `remove` → 'delete'
         * `modify` → compute `simpleDiffRatio(before, after)`; >0.5 → 'strong', else 'minor'
         * `simpleDiffRatio`: if !a return 1; word-overlap = sharedWords(a,b) / wordsB.length; return 1 - overlap
         * Use `toDisplayString` helper that handles string | string[] | object — for the alternatives[0].value extraction, fall back to `''` if missing.
       - Threshold constant `SEVERITY_DIFF_THRESHOLD = 0.5` exported for tests/tunability.

    2. `frontend/src/features/direct-edit/hooks/useChangeHighlights.ts` (NEW):
       - Use the skeleton from RESEARCH.md Code Examples Example 1 (lines 494-619) as the starting point — it is the canonical reference.
       - Track `activeChangeId` via `useState<string | null>(null)`.
       - Memo `pendingChanges` (filter applied + skipped from changes).
       - Memo `severityMap` from changes via inferSeverity (per change, not just pending — this matters because skipped/accepted changes still need their severity for the "all-reviewed" recap).
       - `getRangeForChange` (useCallback) — implement document.createRange + treeWalker per RESEARCH lines 530-585. Use `CSS.escape(change.fieldPath)` in the querySelector to handle bracket characters in paths (e.g., `workExperience[0].bullets[2]`).
       - `documentOrderIds` (useMemo): query `cvContainerRef.current.querySelectorAll('[data-field-path]')`, then sort pendingChanges by indexOf(c.fieldPath) in the resulting array. Falls back to original pendingChanges order when ref is null.
       - `advanceTo` (useCallback): handles null activeChangeId, end-of-list, start-of-list cases.
       - `dismissSection` (useCallback): import `fieldPathToSection` from `../../../utils/formDataPatch`; iterate pendingChanges, filter by section key, return their IDs as a string[]. Plan 04 wires the actual `tailor.skipChange` calls in the consumer — the hook is pure for testability.
       - All returned methods wrapped in useCallback per the project convention (PATTERNS.md §Hook return shape).

    Reference:
    - severity algorithm: RESEARCH.md Pattern 3 (lines 313-345)
    - useChangeHighlights skeleton: RESEARCH.md Code Examples Example 1 (lines 492-619)
    - Hook conventions: PATTERNS.md lines 384-415 + useTailor.ts lines 220-239
    - D-12 (severity rules): CONTEXT.md
    - D-23 (section dismiss): CONTEXT.md
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/__tests__/severity.test.ts src/__tests__/useChangeHighlights.test.ts --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/__tests__/severity.test.ts src/__tests__/useChangeHighlights.test.ts --reporter=basic` exits 0 (both Plan 01 scaffolds now pass).
    - `grep -n "export type Severity = 'strong' | 'minor' | 'add' | 'delete'" frontend/src/features/direct-edit/utils/severity.ts` returns ≥1 match.
    - `grep -n "export function inferSeverity" frontend/src/features/direct-edit/utils/severity.ts` returns ≥1 match.
    - `grep -nE "useCallback|useMemo" frontend/src/features/direct-edit/hooks/useChangeHighlights.ts | wc -l` returns ≥5 (returned helpers wrapped per project convention).
    - `grep -n "CSS.escape(" frontend/src/features/direct-edit/hooks/useChangeHighlights.ts` returns ≥1 match (path query is safe).
    - `grep -n "fieldPathToSection" frontend/src/features/direct-edit/hooks/useChangeHighlights.ts` returns ≥1 match (dismissSection uses canonical mapping).
    - `cd frontend && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    `severity.ts` and `useChangeHighlights.ts` exist; the two Plan 01 scaffolds turn green; tsc clean. Plan 03 and Plan 04 can now consume `inferSeverity`, `Severity`, and the hook's return shape.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EditableField + EditableBulletList highlight extensions</name>
  <files>frontend/src/features/direct-edit/components/EditableField.tsx, frontend/src/features/direct-edit/components/EditableField.module.css, frontend/src/features/direct-edit/components/EditableBulletList.tsx, frontend/src/features/direct-edit/components/EditableBulletList.module.css</files>
  <read_first>
    - frontend/src/features/direct-edit/components/EditableField.tsx (FULL file — current uncontrolled-when-focused effect lines 58-78, handleInput lines 94-99, data-field-path attr line 166)
    - frontend/src/features/direct-edit/components/EditableField.module.css (existing class structure)
    - frontend/src/features/direct-edit/components/EditableBulletList.tsx (FULL file — focus management refs lines 35-56)
    - frontend/src/features/direct-edit/components/EditableBulletList.module.css
    - frontend/src/__tests__/EditableField.test.tsx (existing test — extension surface for D-16 auto-dismiss case)
    - frontend/src/__tests__/EditableBulletList.test.tsx (existing test — pattern for bullet rendering tests)
    - .planning/phases/13-inline-tune-highlights-grammarly-style-review-with-gap-promp/13-RESEARCH.md (lines 280-311 Pattern 2 highlight overlay; lines 348-365 Pattern 4 ghost bullet)
    - .planning/phases/13-inline-tune-highlights-grammarly-style-review-with-gap-promp/13-PATTERNS.md (lines 416-461 EditableField + EditableBulletList self-extend)
    - .planning/phases/13-inline-tune-highlights-grammarly-style-review-with-gap-promp/13-CONTEXT.md (D-13, D-14, D-15, D-16)
    - .planning/phases/13-inline-tune-highlights-grammarly-style-review-with-gap-promp/13-UI-SPEC.md (severity-tier color mapping table; ghost-text spec line 86)
    - frontend/src/styles/variables.css (existing tokens: --error, --error-light, --accent, --accent-light, --success, --success-light, --text-muted, --radius-sm)
  </read_first>
  <behavior>
    EditableField:
    - Existing tests under frontend/src/__tests__/EditableField.test.tsx must continue to pass (no regression).
    - New test (extend EditableField.test.tsx): when `highlightSpans=[{startOffset:0, endOffset:5, severity:'minor', changeId:'c1', isActive:false}]` is passed and the field is NOT focused, the rendered DOM contains a `<span data-change-id="c1" data-severity="minor">` wrapping the first 5 chars.
    - New test: focusing the field strips the highlight spans (innerHTML becomes plain text === value).
    - New test: blurring the field re-injects highlight spans (innerHTML contains the span wrapper again).
    - New test (D-16): with highlightSpans active and `onAutoDismiss` provided, the FIRST `input` event inside the field calls `onAutoDismiss('c1')` exactly once (subsequent inputs do not call it again unless a new highlight appears).
    - tsc clean with verbatimModuleSyntax (use `import type` for HighlightSpan/Severity).

    EditableBulletList:
    - When prop `addChange={ id:'a1', changeType:'add', alternatives:[{value:'New bullet text'}] }` is passed, a `<li>` with `contentEditable="false"` and `data-change-id="a1"` is rendered AFTER the last real bullet.
    - The ghost bullet's text content matches `addChange.alternatives[0].value`.
    - Real bullet count is unchanged (focus management refs unaffected).
    - When `deleteChangeIdsByBulletId` Map indicates bullet 'b2' is delete-targeted, the rendered text for bullet b2 has `text-decoration: line-through` applied via CSS class (verify class presence, not computed style).
  </behavior>
  <action>
    Extend EditableField and EditableBulletList per PATTERNS.md self-extend specs (no rewrites).

    1. `EditableField.tsx`:
       - Add `Severity` import (type-only) and define `HighlightSpan` interface (export it for Plan 04 consumers).
       - Add `highlightSpans?: HighlightSpan[]` and `onAutoDismiss?: (changeId: string) => void` to props.
       - In the existing `useEffect` (lines 58-78) that gates DOM sync on `!isFocused.current`:
         * If `highlightSpans?.length > 0` and `value !== ''`, build innerHTML by injecting `<span class="${className}" data-change-id="..." data-severity="...">` wrappers around the substring `[startOffset, endOffset]` for EACH span. Spans MUST be sorted by `startOffset` ascending and MUST NOT overlap (assume the consumer guarantees non-overlap; if overlap detected via assertion, console.warn and skip the conflicting span — defensive but quiet).
         * The class composition: `styles.highlight ${styles[`tier${capitalizedSeverity}`]} ${isActive ? styles.active : ''}`.
         * If `highlightSpans` is empty/absent, fall back to existing behavior (textContent or innerHTML for rich mode).
         * **CRITICAL** (RESEARCH Pitfall 1): NEVER mutate innerHTML when `isFocused.current === true`. The existing guard already covers this; verify it's preserved.
       - In the existing `handleInput` callback (lines 94-99): if `highlightSpans` non-empty AND `onAutoDismiss` provided, call `onAutoDismiss(span.changeId)` for the span whose offset range contains the cursor (or just the FIRST highlight span — D-16 says "first keystroke inside a highlighted region"). Use a ref `firstInputAfterHighlightRef` to ensure we only fire once per highlight rendering pass.

    2. `EditableField.module.css`:
       - Add `.highlight` rule with `text-decoration-line: underline; text-decoration-style: wavy; text-decoration-thickness: 2px; text-underline-offset: 3px; text-decoration-skip-ink: none; cursor: pointer; transition: background-color 0.15s ease;` per RESEARCH §Pattern 3 example 3 / UI-SPEC §Spacing.
       - Add `.tierStrong` (color: --error), `.tierMinor` (color: --accent), `.tierAdd` (color: --success), `.tierDelete` (line-through, --error).
       - Add `.active.tierStrong { background-color: var(--error-light); }` and parallel rules for tierMinor/tierAdd/tierDelete (UI-SPEC §Color severity-tier table).

    3. `EditableBulletList.tsx`:
       - Add `addChange?: TailorChange` prop (optional). When present, render an extra `<li>` AFTER the existing bullets map per RESEARCH §Pattern 4:
         ```tsx
         {addChange && (
           <li className={styles.ghostBullet} contentEditable={false} data-change-id={addChange.id}>
             <span className={styles.ghostBulletMarker}>+</span>
             <span className={styles.ghostBulletText}>{toDisplayString(addChange.alternatives[0]?.value)}</span>
           </li>
         )}
         ```
       - Add `deleteChangeIdsByBulletId?: Map<string, string>` prop (bulletId -> changeId). For bullets whose id is in the map, pass `highlightSpans: [{startOffset:0, endOffset:bullet.text.length, severity:'delete', changeId, isActive:activeChangeId===changeId}]` to the underlying EditableField. The CSS handles strikethrough via `.tierDelete` class.
       - Pass through `highlightSpansByBulletId?: Map<string, HighlightSpan[]>` to forward modify-type and add-related highlights to individual bullets without further changes here.
       - Existing focus refs (bulletRefs, pendingFocusId) are UNAFFECTED — ghost bullet has no real index and no ref slot.

    4. `EditableBulletList.module.css`:
       - Add `.ghostBullet` rule: `font-style: italic; color: var(--text-muted); background-color: var(--success-light); padding: 0 4px; border-radius: var(--radius-sm); user-select: none;`
       - Add `.ghostBulletMarker { font-weight: 600; color: var(--success); margin-right: 4px; }`
       - Add `.ghostBulletText { font-style: italic; }` (already inherited but explicit for clarity).

    5. Extend `frontend/src/__tests__/EditableField.test.tsx` with the new D-16 + highlightSpans cases described in <behavior> above. Add new `describe('highlightSpans', () => { ... })` block; do not modify existing tests.

    6. Extend `frontend/src/__tests__/EditableBulletList.test.tsx` with a new `describe('addChange ghost bullet', ...)` block covering the contentEditable=false + data-change-id assertions.

    All references:
    - PATTERNS.md lines 416-461 (self-extend EditableField + EditableBulletList)
    - RESEARCH.md Pattern 2 lines 280-311 (highlight overlay rule)
    - RESEARCH.md Pattern 4 lines 348-365 (ghost bullet)
    - RESEARCH.md Pitfall 1 lines 442-447 (cursor lost on innerHTML mutation)
    - RESEARCH.md Pitfall 6 lines 472-476 (ghost text caught by blur sync) — mitigated by contentEditable=false on ghost AND by NOT computing textContent for the ghost when reading bullet text on blur (existing EditableBulletList already reads per-bullet refs, not the parent textContent — verify and preserve).
    - D-13/D-14/D-15/D-16 in CONTEXT.md
    - UI-SPEC §Color severity-tier table (lines 113-119)
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/__tests__/EditableField.test.tsx src/__tests__/EditableBulletList.test.tsx --reporter=basic && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/__tests__/EditableField.test.tsx src/__tests__/EditableBulletList.test.tsx --reporter=basic` exits 0 (existing tests still pass; new tests pass).
    - `cd frontend && npx tsc --noEmit` exits 0.
    - `grep -nE "highlightSpans\?\:" frontend/src/features/direct-edit/components/EditableField.tsx` returns ≥1 match (prop added).
    - `grep -nE "onAutoDismiss\?\:" frontend/src/features/direct-edit/components/EditableField.tsx` returns ≥1 match (D-16 callback wired).
    - `grep -n "data-change-id" frontend/src/features/direct-edit/components/EditableField.tsx` returns ≥1 match.
    - `grep -n "contentEditable={false}" frontend/src/features/direct-edit/components/EditableBulletList.tsx` returns ≥1 match (ghost bullet).
    - `grep -n "ghostBullet" frontend/src/features/direct-edit/components/EditableBulletList.module.css` returns ≥1 match.
    - `grep -nE "text-decoration-style: wavy" frontend/src/features/direct-edit/components/EditableField.module.css` returns ≥1 match.
    - `grep -v '^/\*\|^\s\*' frontend/src/features/direct-edit/components/EditableField.module.css | grep -c "tierStrong\|tierMinor\|tierAdd\|tierDelete"` returns ≥4 (all four severity tiers present).
    - No regression: original EditableField uncontrolled-when-focused contract preserved — `grep -n "isFocused.current" frontend/src/features/direct-edit/components/EditableField.tsx` shows the existing guard intact.
  </acceptance_criteria>
  <done>
    EditableField accepts highlightSpans + onAutoDismiss without breaking the focus contract; squiggle CSS renders the four severity tiers; EditableBulletList renders ghost bullets and routes delete strikethrough; tsc + tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: useTailor + api.ts userClarifications signature extension</name>
  <files>frontend/src/hooks/useTailor.ts, frontend/src/services/api.ts</files>
  <read_first>
    - frontend/src/hooks/useTailor.ts (FULL file — interface line 17, fetchSuggestions implementation lines 91-119, return object lines 220-239)
    - frontend/src/services/api.ts (lines 192-215 — current suggestTailorChanges signature + body construction)
    - frontend/src/__tests__/useImport.test.ts (canonical hook signature test pattern with mocked api)
    - .planning/phases/13-inline-tune-highlights-grammarly-style-review-with-gap-promp/13-PATTERNS.md (lines 539-583 api.ts self-extend; line 583 useTailor.fetchSuggestions caller-update)
    - .planning/phases/13-inline-tune-highlights-grammarly-style-review-with-gap-promp/13-RESEARCH.md (lines 795-815 Code Examples Example 4 frontend block)
    - .planning/phases/13-inline-tune-highlights-grammarly-style-review-with-gap-promp/13-CONTEXT.md (D-07 — userClarifications snake-case wire field)
  </read_first>
  <behavior>
    api.ts:
    - `suggestTailorChanges(formData, jobDescription, companyName, role, userClarifications, signal)` returns the existing TailorResponse shape.
    - When `userClarifications=['x','y']`, the POST body sent to `/api/tailor/suggest-changes` contains `user_clarifications: ['x','y']` (snake_case for FastAPI).
    - When `userClarifications` is undefined, the POST body has `user_clarifications: undefined` (axios omits) — back-compat for callers that don't pass the new arg.
    - AbortSignal still works as the LAST positional arg.

    useTailor:
    - `fetchSuggestions(formData, jobDesc, company, role, userClarifications)` forwards the 5th arg to api.suggestTailorChanges.
    - Existing callers passing only 4 args still type-check (param is optional).
    - `userClarifications=undefined` is forwarded as undefined (no type-coercion of empty array).
    - The `tailor.fetchSuggestions` returned function is still wrapped in useCallback.
    - existing tests for useTailor (if any in __tests__) continue to pass.
  </behavior>
  <action>
    Two minimal signature extensions per PATTERNS.md.

    1. `frontend/src/services/api.ts` — extend `suggestTailorChanges`:
       Add `userClarifications?: string[]` BETWEEN `role?` and `signal?`. In the POST body object, add `user_clarifications: userClarifications` (snake_case) AT THE END so JSON ordering is stable. Reference: PATTERNS.md lines 562-580 + RESEARCH Example 4 third block.

    2. `frontend/src/hooks/useTailor.ts` — extend `fetchSuggestions`:
       - Update the interface declaration (line 17 region) per <interfaces> block above.
       - Update the useCallback body to accept `userClarifications?: string[]` and forward as the 5th arg to `api.suggestTailorChanges(formData, jobDesc, company, role, userClarifications, controller.signal)`.
       - Add `userClarifications` to the useCallback dependency array IF the callback closes over it — otherwise (since it's just forwarded), no dep needed.
       - DO NOT mutate any other useTailor logic. setBaselineScore, applyTailorChanges, etc. are unchanged.

    No new tests are required for this task because:
    - api.ts is exercised by Plan 04's TuneRail test (Task 4 below in Plan 04) integration.
    - useTailor's signature change is type-checked by tsc (verbatim required).
    - However, **if** a useTailor.test.ts exists (verify via `ls frontend/src/__tests__/useTailor*` — none currently), extend it; otherwise add a tiny smoke test inside Plan 04's TuneRail test.

    Cite analog: PATTERNS.md lines 562-580 (api.ts self-extend), line 583 (useTailor caller-update note).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npx vitest run src/__tests__/ --reporter=basic 2>&1 | grep -E "Tests|Files" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx tsc --noEmit` exits 0.
    - `grep -n "userClarifications\?: string\[\]" frontend/src/services/api.ts` returns ≥1 match.
    - `grep -n "user_clarifications: userClarifications" frontend/src/services/api.ts` returns ≥1 match (snake_case wire field).
    - `grep -n "userClarifications\?: string\[\]" frontend/src/hooks/useTailor.ts` returns ≥1 match (interface + callback).
    - `grep -n "fetchSuggestions" frontend/src/hooks/useTailor.ts | wc -l` shows the function still has ≥3 references (declaration, useCallback, return — pattern preserved).
    - The full `cd frontend && npx vitest run src/__tests__/ --reporter=basic` does not show NEW failures introduced by this task (existing tests must still pass; the Plan 01 scaffolds for TuneRail/PostSavePrompt/etc. continue to fail until Plans 03-04 land — that's expected).
  </acceptance_criteria>
  <done>
    `api.suggestTailorChanges` and `useTailor.fetchSuggestions` accept the optional userClarifications arg; tsc clean; no regression in existing tests.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User keystrokes inside CV → React state | Existing — contentEditable input flows through EditableField.handleInput. New: highlight overlay sits inside the same boundary. |
| AI-suggested change content (alternatives[0].value) → DOM via ghost bullet text | New: ghost bullet renders `addChange.alternatives[0].value` as text content. |
| highlightSpans prop ([startOffset, endOffset]) → innerHTML span injection | New: substring offsets must not allow injection of arbitrary HTML. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-01 | Tampering / XSS | EditableField highlight injection (innerHTML mutation) | mitigate | When building the highlighted innerHTML, the executor MUST escape the per-span text content before wrapping with `<span>`. Use a small `escapeHtml(s) → s.replace(/[&<>"']/g, ...)` helper inside the effect, OR build via `document.createTextNode` + `appendChild` rather than string concatenation. The current uncontrolled-when-focused code uses plain `textContent` (safe). The new highlight branch is the only place innerHTML is built from user-typed strings — escape is mandatory. ASVS V5.3.3. |
| T-13-02-02 | Tampering / XSS | Ghost bullet renders alternatives[0].value as React JSX text | accept (mitigated by JSX) | React JSX `{addChange.alternatives[0].value}` already escapes HTML. No additional mitigation needed. ASVS V5.3.3 — defense via framework. |
| T-13-02-03 | Information Disclosure | Sending userClarifications over the wire | accept | Same channel as the existing JD payload. Already authenticated via X-User-Id header (Phase 5+). No new exposure beyond the JD itself. ASVS V9.1.1 — TLS in production deployment. |
| T-13-02-04 | Tampering | DOM Range computed from user-controlled fieldPath | mitigate | `useChangeHighlights.getRangeForChange` uses `CSS.escape(fieldPath)` before passing to querySelector, defending against fieldPath strings that contain CSS-special characters (which AI could in principle return). ASVS V5.1.4. |

**High-severity threats blocking the plan:** T-13-02-01 — must escape per-span text before innerHTML injection. Mitigation is mandatory and blocks merge if absent.
</threat_model>

<verification>
- `cd frontend && npx vitest run src/__tests__/severity.test.ts src/__tests__/useChangeHighlights.test.ts src/__tests__/EditableField.test.tsx src/__tests__/EditableBulletList.test.tsx --reporter=basic` → all green.
- `cd frontend && npx tsc --noEmit` → exits 0.
- `cd frontend && npx eslint . --max-warnings 0 src/features/direct-edit/utils/severity.ts src/features/direct-edit/hooks/useChangeHighlights.ts src/features/direct-edit/components/EditableField.tsx src/features/direct-edit/components/EditableBulletList.tsx` → exits 0.
- Manual grep: `grep -n "innerHTML" frontend/src/features/direct-edit/components/EditableField.tsx` shows escapeHtml or createTextNode usage (T-13-02-01 mitigation).
</verification>

<success_criteria>
- inferSeverity returns the four severity values per D-12 word-overlap rules (>0.5 → strong)
- useChangeHighlights returns a hook surface that the popover and rail can drive
- EditableField accepts highlightSpans without breaking existing focus contract; squiggle renders for all four tiers
- EditableField onAutoDismiss fires once per highlight rendering pass on first keystroke
- EditableBulletList renders ghost bullets (contentEditable=false) for add-type changes
- useTailor.fetchSuggestions and api.suggestTailorChanges forward userClarifications: string[] to backend (snake_case wire field)
- All tests green, tsc clean, eslint clean
- T-13-02-01 mitigation present (escapeHtml or createTextNode for innerHTML span injection)
</success_criteria>

<output>
After completion, create `.planning/phases/13-inline-tune-highlights-grammarly-style-review-with-gap-promp/13-02-SUMMARY.md` summarizing:
- New files (severity.ts, useChangeHighlights.ts)
- Modified files (EditableField, EditableBulletList, useTailor, api.ts)
- Test scaffolds turned green (severity, useChangeHighlights, plus extended EditableField + EditableBulletList tests)
- Open Plan 03/04 contract handoffs (HighlightSpan interface; onAutoDismiss wiring; ghost bullet props)
- Notes on T-13-02-01 mitigation approach (escapeHtml or createTextNode)
</output>
