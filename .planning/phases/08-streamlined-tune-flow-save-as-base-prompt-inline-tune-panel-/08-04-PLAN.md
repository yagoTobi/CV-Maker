---
phase: 08-streamlined-tune-flow
plan: 04
type: execute
wave: 3
depends_on: [08-02, 08-03]
files_modified:
  - frontend/src/features/apply-to-job/ApplyToJobScreen.tsx
  - frontend/src/features/apply-to-job/ApplyToJobScreen.module.css
autonomous: true
requirements: [TUNE-06]

must_haves:
  truths:
    - "ApplyToJobScreen.tsx and ApplyToJobScreen.module.css are deleted"
    - "No production code imports ApplyToJobScreen"
    - "All existing tests pass after deletion"
  artifacts: []
  key_links: []
---

<objective>
Delete the now-unused ApplyToJobScreen component and its CSS module. The inline TunePanel (Plan 03) has fully replaced this component, and the /apply route (Plan 02) redirects to /build/form.

Purpose: Dead code removal. ApplyToJobScreen is no longer imported by any production code after Plan 02 removed its lazy import from App.tsx.
Output: Two files deleted: ApplyToJobScreen.tsx, ApplyToJobScreen.module.css
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-streamlined-tune-flow-save-as-base-prompt-inline-tune-panel-/08-CONTEXT.md
@.planning/phases/08-streamlined-tune-flow-save-as-base-prompt-inline-tune-panel-/08-02-SUMMARY.md
@.planning/phases/08-streamlined-tune-flow-save-as-base-prompt-inline-tune-panel-/08-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify no imports and delete ApplyToJobScreen</name>
  <read_first>
    - frontend/src/App.tsx
    - frontend/src/features/apply-to-job/ApplyToJobScreen.tsx
  </read_first>
  <files>frontend/src/features/apply-to-job/ApplyToJobScreen.tsx, frontend/src/features/apply-to-job/ApplyToJobScreen.module.css</files>
  <action>
1. First, run a dead code audit to confirm no file imports ApplyToJobScreen:
```bash
grep -rn "ApplyToJobScreen" frontend/src/ --include="*.tsx" --include="*.ts"
```
Expected result: Zero matches (the lazy import was removed in Plan 02).

If any imports are found, do NOT delete -- report the imports as a blocker.

2. If zero imports confirmed, delete both files:
```bash
rm frontend/src/features/apply-to-job/ApplyToJobScreen.tsx
rm frontend/src/features/apply-to-job/ApplyToJobScreen.module.css
```

3. Check if the `features/apply-to-job/` directory is now empty. If it is, delete the directory:
```bash
ls frontend/src/features/apply-to-job/
# If empty, remove:
rmdir frontend/src/features/apply-to-job/
```

4. Run the full test suite to confirm nothing breaks:
```bash
cd frontend && npx vitest run --reporter=verbose 2>&1 | tail -30
```

5. Run TypeScript type check:
```bash
cd frontend && npx tsc --noEmit 2>&1 | tail -20
```
  </action>
  <verify>
    <automated>cd /Users/yagotobi/Documents/Code/GitHub/Projects/CV-Maker && test ! -f frontend/src/features/apply-to-job/ApplyToJobScreen.tsx && test ! -f frontend/src/features/apply-to-job/ApplyToJobScreen.module.css && echo "Files deleted successfully" && grep -rn "ApplyToJobScreen" frontend/src/ --include="*.tsx" --include="*.ts" || echo "No surviving imports"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/features/apply-to-job/ApplyToJobScreen.tsx does NOT exist
    - frontend/src/features/apply-to-job/ApplyToJobScreen.module.css does NOT exist
    - `grep -rn "ApplyToJobScreen" frontend/src/ --include="*.tsx" --include="*.ts"` returns zero matches
    - `cd frontend && npx vitest run` exits with code 0
    - `cd frontend && npx tsc --noEmit` exits with code 0 (or only pre-existing warnings)
  </acceptance_criteria>
  <done>ApplyToJobScreen and its CSS module are deleted. No production code references them. Test suite and type check pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan only deletes unused files. No trust boundaries affected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Denial of Service | Dead code removal | accept | Files confirmed unused via grep before deletion. No runtime impact. |
</threat_model>

<verification>
- `test ! -f frontend/src/features/apply-to-job/ApplyToJobScreen.tsx` succeeds
- `grep -rn "ApplyToJobScreen" frontend/src/` returns zero matches
- `cd frontend && npx vitest run` passes
- `cd frontend && npx tsc --noEmit` passes
</verification>

<success_criteria>
- ApplyToJobScreen.tsx and ApplyToJobScreen.module.css deleted
- Zero surviving imports of the deleted component
- Full test suite green
- TypeScript compilation clean
</success_criteria>

<output>
After completion, create `.planning/phases/08-streamlined-tune-flow-save-as-base-prompt-inline-tune-panel-/08-04-SUMMARY.md`
</output>
