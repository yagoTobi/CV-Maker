"""Domain models package.

Holds Pydantic data models that describe the CV domain (`CVFormData`, version
metadata, etc.). Routes and services should import from here rather than from
each other to avoid inverted architectural arrows.
"""
