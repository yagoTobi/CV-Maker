%% Deedy Resume Template
%% Generated by CV Maker
%% IMPORTANT: Compile with XeLaTeX

\documentclass[]{deedy-resume-openfont}
\usepackage{fancyhdr}

\pagestyle{fancy}
\fancyhf{}

\begin{document}

(% set name_parts = personal.fullName.split(' ', 1) %)
\namesection{(( name_parts[0] | latex_escape ))}{(% if name_parts | length > 1 %)(( name_parts[1] | latex_escape ))(% endif %)}{%
(% set contact_parts = [] -%)
(% if personal.email -%)(% set _ = contact_parts.append('\href{mailto:' + (personal.email | latex_url_escape) + '}{' + (personal.email | latex_escape) + '}') -%)(% endif -%)
(% if personal.phone -%)(% set _ = contact_parts.append(personal.phone | latex_escape) -%)(% endif -%)
(% for link in personal.links -%)(% set _ = contact_parts.append('\href{' + (link.url | latex_url_escape) + '}{' + (link.label | latex_escape) + '}') -%)(% endfor -%)
(% if personal.location -%)(% set _ = contact_parts.append(personal.location | latex_escape) -%)(% endif -%)
(( contact_parts | join('\,|\,') ))}

\begin{minipage}[t]{0.33\textwidth}

(% if education %)
\section{Education}
(% for edu in education %)
\subsection{(( edu.school | latex_escape ))}
\descript{(( edu.degree | latex_escape ))}
\location{(( edu.startDate | latex_escape ))(% if edu.endDate %)\,--\,(( edu.endDate | latex_escape ))(% endif %)(% if edu.location %)\,|\,(( edu.location | latex_escape ))(% endif %)(% if edu.gpa %)\\ GPA: (( edu.gpa | latex_escape ))(% endif %)}
(% for detail in edu.details %)(( detail | latex_escape ))\\(% endfor %)
\sectionsep
(% endfor %)
(% endif %)

(% if skills %)
\section{Skills}
(% for cat in skills %)
\subsection{(( cat.category | latex_escape ))}
(( cat.skills | map('latex_escape') | join(' \\textbullet{} ') ))
\sectionsep
(% endfor %)
(% endif %)

(% if awards %)
\section{Awards}
\begin{tabular}{rll}
(% for award in awards %)
(( award.year | latex_escape )) & (( award.title | latex_escape ))(% if award.description %): (( award.description | latex_escape ))(% endif %) \\
(% endfor %)
\end{tabular}
\sectionsep
(% endif %)

\end{minipage}
\hfill
\begin{minipage}[t]{0.66\textwidth}

(% if work %)
\section{Experience}
(% for job in work %)
\runsubsection{(( job.company | latex_escape ))}
\descript{|\,(( job.title | latex_escape ))}
\location{(( job.startDate | latex_escape ))(% if job.endDate %)\,--\,(( job.endDate | latex_escape ))(% endif %)(% if job.location %)\,|\,(( job.location | latex_escape ))(% endif %)}
(% if job.bullets %)
\begin{tightemize}
(% for bullet in job.bullets %)
\item (( bullet | latex_escape ))
(% endfor %)
\end{tightemize}
(% endif %)
\sectionsep
(% endfor %)
(% endif %)

(% if projects %)
\section{Projects}
(% for proj in projects %)
\textbf{(( proj.name | latex_escape ))}(% if proj.year %)\,(( proj.year | latex_escape ))(% endif %): (( proj.description | latex_escape ))(% if proj.technologies %)\,\textit{(( proj.technologies | latex_escape ))}(% endif %)
(% if proj.bullets %)
\begin{tightemize}
(% for bullet in proj.bullets %)
\item (( bullet | latex_escape ))
(% endfor %)
\end{tightemize}
(% else %)
\vspace{3pt}
(% endif %)
(% endfor %)
\sectionsep
(% endif %)

(% for asec in additional_sections %)
\section{(( asec.title | latex_escape ))}
(% for entry in asec.entries %)
\runsubsection{(( entry.title | latex_escape ))}
(% if entry.subtitle %)\descript{|\,(( entry.subtitle | latex_escape ))}(% endif %)
(% if entry.startDate %)\location{(( entry.startDate | latex_escape ))(% if entry.endDate %)\,--\,(( entry.endDate | latex_escape ))(% endif %)(% if entry.location %)\,|\,(( entry.location | latex_escape ))(% endif %)}(% endif %)
(% if entry.description %)(( entry.description | latex_escape ))\\(% endif %)
(% if entry.bullets %)
\begin{tightemize}
(% for bullet in entry.bullets %)
\item (( bullet | latex_escape ))
(% endfor %)
\end{tightemize}
(% endif %)
\sectionsep
(% endfor %)
(% endfor %)

\end{minipage}
\end{document}
